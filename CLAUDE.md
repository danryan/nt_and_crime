# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repo is

A compatibility shim that lets near-unmodified Phazerville Hemisphere applet sources compile and run as Expert Sleepers disting NT C++ plug-ins. Vendor source is pinned via git submodules. The standing rule is that vendor source is not hand-edited and is never committed-modified inside the submodule (the pin SHA does not change). That rule now has ONE narrow, documented exception: a build-time provisioning patch under `vendor-patches/`, applied by `bootstrap.sh` to the vendor working tree and never committed into the submodule. See "Vendor provisioning patches" below for what it removes and why shim shadowing could not cover it. Everything else project-specific lives in `shim/` and `plugins/`. The top-level `applets/` directory and the bundled-host build were removed in the 2026-05-23 cleanup release; per-applet plug-ins under `plugins/applets/` plus composer hosts under `plugins/hosts/` are the only on-device shape.

## Bootstrap

```sh
./bootstrap.sh        # verifies toolchain + python deps, provisions all submodules
make vendor           # runs ./bootstrap.sh (canonical submodule provisioning entry)
```

Required tools: `arm-none-eabi-c++` (for the NT target), `clang++` or `g++` (host), `python3`. `bootstrap.sh` contains OS-specific install hints for macOS and Debian/Ubuntu. Three submodules: `vendor/distingNT_API` (Expert Sleepers SDK), `vendor/O_C-Phazerville` (Hemisphere applet sources, pinned at SHA `7800d929`; verify with `git ls-tree HEAD vendor/O_C-Phazerville`), and `vendor/llvm-project` (compiler-rt builtins, pinned at tag `llvmorg-19.1.0`, sparse-checkout limited to `compiler-rt/lib/builtins/`). `make vendor` delegates to `bootstrap.sh` because the sparse-checkout config for `vendor/llvm-project` is NOT carried by a plain `git submodule update`; the bootstrap block clones it sparsely and re-applies sparse config on every run. See "Adding a sparse-checkout vendor submodule" below.

## Build and test commands

| Command | Purpose |
| --- | --- |
| `make help` | List targets |
| `make arm` | Build all NT plug-ins under `build/arm/*.o` (the deployable artifact) |
| `make host` | Build host simulator binary |
| `make test-applets` | Run per-applet Catch2 host test binaries (alias for `test-applets-pilot`; the main test target during applet ports) |
| `make test-runtime` | Build and run NT runtime simulator tests |
| `make test-buses`, `test-draw`, `test-draw-shape`, `test-json`, `test-params`, `test-loader` | Per-subsystem host tests |
| `make test` | Run host build + applet tests + a scripted scenario |
| `make deploy DEVICE=/Volumes/NT` | Copy `build/arm/*.o` to a mounted NT in USB disk mode |
| `make deploy-sysex SYSEX_PLUGIN=build/arm/Hemispheres_host.o SYSEX_ID=0` | Push a built plug-in over USB-MIDI sysex (NT firmware v1.13+, no reboot) |
| `make clean` | Remove `build/` |

To run a single Catch2 test case, pass a tag to the matching per-applet binary directly: `./build/host/test_applet_Cumulus '[cumulus]'` (build first via `make build/host/test_applet_Cumulus`).

## Architecture

```
vendor/O_C-Phazerville/      vendor/distingNT_API/
   (Hemisphere applets,         (NT plug-in ABI:
    vendored unmodified)         _NT_algorithm,
            │                    _NT_parameter,
            ▼                    factory entry)
   shim/include/                       │
   (HemisphereApplet,                  │
    HSUtils, HSicons,                  ▼
    Phzicons, bus I/O)  ──►  plugins/applets/<APPLET>.cpp
                              (one per-applet .o each)
                                       │
                              plugins/hosts/*_host.cpp
                              (composer hosts load
                               per-applet plug-ins at
                               runtime via HemiPluginInterface)
                                       │
                                       ▼
                              harness/  or  device
                              (Catch2,      (deploy via
                               nt_runtime    USB-MIDI
                               simulator)    sysex or
                                             USB disk)
```

The project has three layers (vendor, shim, plugins) plus the harness.

**`vendor/` (effectively read-only):** Submodules. `vendor/distingNT_API` provides the NT plug-in ABI (`_NT_algorithm`, `_NT_parameter`, factory entry points). `vendor/O_C-Phazerville/software/src/applets/*.h` provides Hemisphere applet sources, vendored unmodified. The shim's contract is that any applet header you find in `vendor/O_C-Phazerville/software/src/applets/` must compile against `shim/include/` without edits to the vendor source. The one exception to "no vendor edits" is the provisioning-patch mechanism (see "Vendor provisioning patches"): a tiny diff applied to the working tree at bootstrap time, never committed into the submodule, used only where shim shadowing structurally cannot win. Treat shim shadowing as the default; reach for a provisioning patch only after proving shadowing cannot work.

**`shim/include/` (the translation layer):** Headers that satisfy what Hemisphere applets reference (`HemisphereApplet` base class, `HSUtils.h` macros, `HSicons.h` shared icons, `PhzIcons.h` applet-specific icons, `HSClockManager.h`, `HSIOFrame.h`, `OC_core.h`, `OC_DAC.h`, `Arduino.h` stubs, the bus I/O API: `In`, `Out`, `Clock`, `Gate`, `ClockOut`, `GateOut`). Implementation in `shim/src/` (globals, graphics, icon bitmaps). `docs/shim-additions.md` is the per-applet ledger of what each port required beyond the previous baseline, audited so the shim surface stays minimized.

**`plugins/` (the deployable units):** Each `plugins/applets/<APPLET>.cpp` is one NT plug-in compiling to one small `.o` (16-20 KB `.text`); it includes its manifest at `shim/include/applet_manifests/<APPLET>.h`, the per-applet runtime header `plugins/applets/_per_applet_runtime.h`, and the vendor applet header directly. `plugins/hosts/Hemispheres_host.cpp` (GUID `HmHh`, 2 lanes) and `plugins/hosts/Quadrants_host.cpp` (GUID `QdHh`, 4 lanes) are composer hosts that load per-applet plug-ins at runtime through the firmware `_NT_slot` API plus the versioned `HemiPluginInterface` function-pointer-in-data ABI. To add an applet, create its `plugins/applets/<APPLET>.cpp` plus `shim/include/applet_manifests/<APPLET>.h`, add it to `ALL_APPLET_LIST` in the Makefile, and add icon stubs in `shim/include/PhzIcons.h` plus `shim/src/icons.cpp` if needed.

**`harness/` (host test infrastructure):** `harness/src/nt_runtime.cpp` simulates the NT's audio frame loop in-process. `harness/src/plugin_loader.cpp` loads plug-ins through the same factory path as the device. `harness/tests/test_applet_<APPLET>.cpp` is the per-applet Catch2 binary used for applet behavior coverage; each file carries its own local `pack_<applet>` helper that mirrors the vendor `OnDataRequest` byte-by-byte (see "Pack helper convention" below). `harness/tests/test_buses.cpp`, `test_draw_text.cpp`, etc. cover non-applet subsystems. `harness/scripts/run_scenario.py` runs YAML-driven integration scenarios from `tests/scenarios/`.

## Critical gotcha: 10x clocked multiplier

The host harness runs the vendor `Controller()` 10 times per NT `step()` call (`ticks_this_step = numFrames / 3 = 10`). A single rising edge on `Clock(side)` sets `clocked[side] = true` once, but `clocked[side]` stays asserted across all 10 inner Controller calls in that buffer. Any applet whose Controller advances an internal counter, accumulator, or toggle inside `if (Clock(ch))` will fire 10 times per buffer per edge, not once.

Bus-level "fires once per N input edges" assertions are unreliable for these applets. Two valid coverage shapes:

1. Model the multiplier explicitly in the assertion math. See Cumulus per-applet test `harness/tests/test_applet_Cumulus.cpp` for the canonical commentary and Stairs ST3 / RunglBook RB3 for working examples.
2. Drop bus-level fire-count assertions; cover via round-trip plus state-injection only. See ProbabilityDivider PD3/PD4 for the template.

Per-applet entries in the design specs must state which shape they use. New applet ports MUST acknowledge this rule in their test concerns or test failures will look mysterious.

## Vendor compat headers compile against shim

Vendor non-applet headers (`HSProbLoopLinker.h`, `SegmentDisplay.h`, etc.) usually compile against the shim without needing a shim stub. They depend on `graphics` (already global in `hem_graphics.h`), `HEM_SIDE` (already in `HSUtils.h` via `using namespace HS;`), and `<cstdint>` types — all in scope by the time the include chain reaches them. Check whether the vendor header compiles as-is before preemptively writing a stub.

## Proportion is a free function (not a member)

`Proportion(numerator, denominator, max_value)` lives in `shim/include/util/util_math.h` as a free function mirroring vendor `util/util_math.h:48`. Vendor applets with nested helper structs (ADSREG's `MiniADSR`) call `Proportion(...)` unqualified; name lookup from inside a nested class binds to the enclosing class's inherited `HemisphereApplet::Proportion` as a non-static member and fails. Adding `Proportion` as a member of `HemisphereApplet` re-introduces this hazard. Keep it free.

## Changed() and changed_cv update

`HemisphereApplet::Changed(int ch)` reads `HS::frame.changed_cv[ch + channel_offset()]`. The shim populates `changed_cv[i]` in `step()` by comparing `inputs[i]` against a static `last_cv[4]`; a channel's flag flips when the delta exceeds `HEMISPHERE_CHANGE_THRESHOLD = 32` hem units (~1/8 semitone, vendor `HSUtils.h:25`).

## Single-shot gate tests must clear the bus between steps

`set_gate(bus, side, ch, 0, 8)` writes a 1-sample pulse at frame 0. Subsequent `step()` calls see the same bus and the shim's rising-edge detector refires every step unless `clear_bus(bus)` runs between. Tests that need a single `Clock` edge then a long step window MUST clear the bus after the edge-firing step, otherwise `StartADCLag` re-runs each buffer and `EndOfADCLag` never trips.

## IOFrame array-initializer quirk

`bool/int arr[4] = { -1 };` initializes ONLY `arr[0]` to -1; `arr[1..3]` default-initialize to 0. The shim's `adc_lag_countdown[4] = { -1 };` in `HSIOFrame.h` is the load-bearing example. Be careful when reading initial state of any per-channel array in the shim.

## Pack helper convention

`pack_<applet>` helpers in each `harness/tests/test_applet_<APPLET>.cpp` mirror the vendor `OnDataRequest` byte-by-byte. Each per-applet test owns its own local copy of the relevant helper. Rules:

- Use `int` for each field at the helper boundary; apply the vendor bias inside (e.g., `(value + 32)` for ClockDivider's `div[i] + 32`).
- AND with the field-width mask, not `0xFF`.
- Explicitly zero gap bits the vendor `OnDataRequest` skips (Cumulus zeroes bits 11..12; Voltage zeroes bit 9). Round-trip stability depends on this.
- Applets whose `OnDataRequest()` returns 0 (Button, GatedVCA, Switch) get no pack helper; their tests assert `OnDataRequest() == 0` directly.

## Document layout

Non-trivial changes produce three auditable documents:

- `docs/superpowers/brainstorms/YYYY-MM-DD-<topic>-brainstorm.md` (scope, vendor SHA, categorization, status)
- `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md` (canonical recipe + per-item entries + spec footer with Recipe spot-check, Per-entry verification, Shim prereq verification)
- `docs/superpowers/plans/YYYY-MM-DD-<topic>-plan.md` (worklist; parallel by default; sequenced only where dependencies justify it)

Retired topic docs are consolidated under `docs/superpowers/archived/<topic>/` with a per-topic `INDEX.md`. Abort reports live under `docs/superpowers/abort-reports/`. The Phase 3 attempt-1 retrospective (`2026-05-18-phase3-attempt-1-retrospective.md`) and the ResetClock spec-mismatch report (`2026-05-18-resetclock-spec-mismatch.md`) are still required reading before any parallel subagent fan-out: they encode the failure modes (dispatching from `main` instead of the feature branch; enforcing the implementer contract via prose instead of a hook) that the worktree-dispatch discipline now guards against.

## Parallel execution

The project rule at `~/.claude/rules/parallel-execution.md` is load-bearing. Independent units are parallelized via isolated worktrees plus subagent dispatch in a single message. End-to-end wallclock equals the slowest single unit plus integration, not the sum. Dispatch each implementer in a worktree branched from the feature branch, never from `main`. After `git worktree add` (or `EnterWorktree`), run `git submodule update --init --recursive --depth=1` in the new worktree before any build; worktrees do not inherit submodule state.

## Workflow

Standard sequence for any non-trivial change:

1. **Brainstorm** the scope under `docs/superpowers/brainstorms/`. Vendor SHA at the top, status per item, exclusions named.
2. **Spec** under `docs/superpowers/specs/` with the canonical recipe plus per-item entries plus the spec footer (Recipe spot-check, Per-entry verification, Shim prereq verification).
3. **Plan** under `docs/superpowers/plans/` declaring parallelism, inlining the worktree-dispatch checklist.
4. **Fan-out** via `superpowers:subagent-driven-development`. One implementer subagent per unit, each in its own worktree branched from the feature branch (never from `main`).
5. **Integration** on the feature branch: cherry-pick implementer commits, run `make test-applets` and `make arm`.
6. **PR** opened by the feature branch. Hardware smoke check happens after PR open since it needs physical access.

## Audit disciplines

- Load-bearing infrastructure (helpers, shim subsystems) is designed in the spec, not improvised under deadline in the brainstorm. Brainstorms categorize and select; they do not design.
- When auditing a worklist, a high rate of items needing deferral is evidence the boundary is mis-drawn, not just that scope is small. Halt and reassess the boundary rather than shrinking item-by-item.

## NT plug-in runtime

PIC plug-in builds require all-PIC linkage. The NT firmware applies relocations at on-device link time and treats `-fPIC` plug-ins as the expected shape. Mixing PIC plug-in code with non-PIC ARM asm (e.g., from `libgcc.a` extraction) produces "relocation of non-loaded section" warnings on every load. Stock `arm-none-eabi-gcc` ships no PIC `libgcc` multilib for `v7e-m+dp/hard` (verify with `arm-none-eabi-gcc -fPIC -print-libgcc-file-name` returning the same path as without `-fPIC`). Solution: source compiler-rt builtins from `vendor/llvm-project/compiler-rt/lib/builtins/` (submodule pinned at `llvmorg-19.1.0`, sparse-checkout limited to that tree), compile each `.c` file with `arm-none-eabi-gcc` (NOT `c++` — C++ name-mangles EABI symbol names), and partial-link via `ld -r --strip-debug`. Apache-2.0 with LLVM exception licensing avoids GPL drag. `cxx_runtime_stubs.cpp` handles the C++ ABI symbols the runtime doesn't link: `__aeabi_atexit` (no-op), `__dso_handle` (nullptr), operator `new` (returns nullptr, arm-only), `std::__throw_bad_function_call` (spin, arm-only). NT firmware also doesn't provide certain vendor static class members: `SegmentDisplay::digit` is declared `static constexpr uint8_t digit[10]` but never defined out-of-class (C++11/14 odr-use bug); shim provides the out-of-class definition in `shim/src/globals.cpp`. `plugins/probes/aeabi_probe.cpp` is the permanent diagnostic for confirming which firmware symbols are unresolved after toolchain or vendor updates: deploy it via `make deploy-sysex SYSEX_PLUGIN=build/arm/aeabi_probe.o` and read the NT screen's first unresolved-symbol error.

Diagnostic discipline beats guessing. When stripping sections in sequence doesn't fix a "non-loaded section" error, the next move is `arm-none-eabi-objdump -r build/arm/Hemispheres_host.o | awk '$2 ~ /^R_ARM/ {print $2}' | sort | uniq -c` to enumerate actual relocation types, not another strip flag. The relocation table tells you what's wrong: `R_ARM_GOT32`/`R_ARM_GOTPC` reveal PIC code, `R_ARM_ABS32` reveals direct addressing. Mixed in a single `.o` is the failure pattern. Hardware deploys are slow; one minute reading `objdump -r` saves an hour of strip-and-redeploy cycles.

## NT firmware .text budget (~82KB per .o, scan-time)

NT firmware refuses to register a plug-in whose `.text` exceeds approximately 82KB. Misc > Plug-ins > View Info shows "Not enough memory for .text : <name>" and the entry as Failed. Empirically: 50KB loads, 81566 B loads, 83448 B fails. Section count is NOT the cap; the same firmware accepted a 12-section build at 81KB and refused a 14-section build at 83KB. Each `.o` is checked independently. The per-applet model keeps every plug-in small (16-20 KB `.text`), so the cap is no longer pressing for the shipped set; it matters when adding a heavy single applet or growing a composer host.

## Runtime ITC pool is shared across loaded slots

Two limits, not one:

- Scan-time cap (~82KB `.text` per `.o`): described above.
- Run-time pool (shared across all loaded algorithm instances): each slot loads its own copy of plug-in `.text` into ITC RAM. Total ITC across slots must fit a hardware budget (empirically ~100KB). With the per-applet model each loaded applet contributes only its own 16-20 KB, so many applets coexist; the limit is reached only by loading many slots at once. Error path is identical: "Not enough memory for .text : <name>" when adding an algorithm to a preset.

NT API has no mechanism to share `.text` across plug-ins (`calculateStaticRequirements` shares DATA only).

## Diagnosing failed plug-in loads

Misc > Plug-ins > View Info shows pass/fail per `.o` with ITC/DTC/DRAM stats. It does NOT expand a per-plugin failure reason on a Failed entry (the screen stays at the summary); bisect plug-in size against the `.text` cap by iterating builds and screenshotting instead. Use `mcp__nt_helper__show_screen` to capture screen state in-session after `make deploy-sysex`. `aeabi_probe.cpp` remains the diagnostic for unresolved-symbol errors.

nt_helper MCP has no plug-in-file-upload tool (preset/param/slot/routing/screen only); push `.o` files with `make deploy-sysex`. `push_plugin_to_device.py` auto-selects a free `disting NT` USB-MIDI port (`NT_SYSEX_PORT` overrides the port name), so the upload can run alongside a connected nt_helper when the NT exposes more than one port. macOS CoreMIDI endpoints are shareable, so with a single endpoint the open always succeeds and the real contention is protocol-level: the upload is a sysex request/ACK handshake on the IN port that concurrent nt_helper sysex traffic can corrupt. If it fails ("Error uploading plug-in file!"), pause nt_helper activity or disconnect it for the upload. Its `edit_parameter` rejects numeric values ("requires a numeric value") because the untyped value field is stringified in transit; only enum-string edits work over MCP. Batch `new([a,b])` intermittently drops the second algorithm ("did not appear"); a single `add()` or a retry is reliable. nt_helper builds its algorithm catalog at connect and does NOT refresh it after a device reboot or plug-in rescan: `new`/`add` by a freshly-deployed GUID returns "Algorithm with GUID X not found in the list of available algorithms" even though the device has registered it. This is an nt_helper-side staleness artifact, not device truth; run nt_helper's rescan-plugins command to re-enumerate (a plain disconnect/reconnect did NOT refresh the catalog in practice), or confirm registration on the hardware UI.

To confirm a plug-in REGISTERED (vs silently rejected), enumerate over sysex rather than reading the screen: command `0x30` returns the algorithm count, `0x31 <index>` returns name, 4-byte GUID, and an `isPlugin` byte (see the upstream `thorinside/nt_helper` `docs/SYSEX_REFERENCE.md`, distilled locally in `docs/nt-sysex-protocol.md`). Match your GUID (e.g. `HmHh`, `QdHh`) in the enumeration. There is NO sysex opcode that returns a scan-failure reason; a rejected plug-in simply does not appear in the `0x31` list.

## Loader does not honor custom code sections

NT firmware loader copies the canonical `.text` section verbatim into a fixed ITC buffer at registration. It does NOT recognize non-canonical executable section names (e.g., `.code_dram`, `.text.cold`). Functions tagged with `__attribute__((section(".code_dram")))` end up at unmapped addresses; the plug-in may register but adding it to a preset hard-faults the device when `step()` jumps into the unmapped region. Confirmed empirically with `plugins/probes/section_probe.cpp`: probe registered cleanly, hard-crashed on algorithm add. Power-cycle to recover; remove the bad `.o` via USB disk mode before next boot.

Practical consequence: there is no way to route cold methods to DRAM/OCRAM to dodge the per-`.o` `.text` cap. The cap is binding. Strategies that target it: aggressive code shrink (Q15 LUTs, kill `printf`, drop unused vendor deps) and, for a single applet that overflows, splitting its work. The per-applet model already keeps each `.o` small. The `section_probe.cpp` diagnostic stays in tree so future firmware updates can be re-verified.

## Construct-time parameterChanged hazard

Firmware fires `parameterChanged` for each parameter during the algorithm's construct path, before the algorithm is fully registered in the preset's slot table. Calling `NT_setParameterFromUi` back into self from inside that spurious `parameterChanged` hard-crashes the device on add-algorithm. Guards on `self->v != nullptr` and `NT_algorithmIndex(self) >= 0` are NOT sufficient; both return valid values during the construct-time fire. Confirmed via `plugins/probes/reentrancy_probe.cpp` (see `docs/superpowers/prompts/2026-05-20-host-ux-rework-kickoff.md` Reentrancy result).

Mitigation pattern: a sentinel on the instance that flips true only after the algorithm is genuinely alive (e.g. `customUi` arming a flag just before forwarding, or `draw_count > 0` after `draw()` has run at least once). `parameterChanged` only forwards when the sentinel passes; construct-time spurious calls are dropped.

Separate firmware result from the same probe (load-bearing for proxy design): `NT_setParameterFromUi` does NOT re-enter `parameterChanged` synchronously (NEST counter stays at 0). Firmware defers the downstream notify to a later call frame. No stack-safety re-entry guard required.

## numParameters must match actual valid table range

Firmware reads `inst->parameters[i]` for `i` in `[0, numParameters)` and does not validate each entry. If `numParameters` is set to a buffer's max capacity but only `M` entries are populated, entries `[M, numParameters)` are zero-init `_NT_parameter{nullptr, 0, 0, 0, kNT_unitNone, 0, nullptr}` and become phantom parameters on the algo page (blank name, unsettable). For hosts with a dynamic parameter table sized to a worst-case Quadrants budget but K = 2 lanes used, the correct value is `K + K * kMaxProxyParamsPerSlot`, NOT `kMaxHostParams`. See `plugins/hosts/Hemispheres_host.cpp::calculateRequirements_impl` for the canonical formulation.

## Host-test seam: *_test_inject_slot

Three injection tables exist for host tests: `hh_test_inject_slot` (Hemispheres host), `qq_test_inject_slot` (Quadrants host), `host_proxy::hp_test_inject_slot` (shared aggregator). All gate on `NT_HEM_HOST_SIM` (set in `HOST_FLAGS`). Tests populate the table; the host's slot-resolution path reads from it instead of firmware `NT_getSlot`. Production code is unchanged. New host plug-in tests reuse the matching seam; do not invent a fourth.

## Catch2 main lives in harness/src/catch_main.cpp

Test files must not `#define CATCH_CONFIG_MAIN` or define `int main()`. The shared `harness/src/catch_main.cpp` (linked via `HARNESS_SRCS`) supplies Catch2's main entry point. Defining a second main causes a linker collision. Test files include `"catch.hpp"` and declare `TEST_CASE(...)` only.

## _NT_factory designated-initializer order

`_NT_factory` field order matters with C++20 designated initializers. The struct orders `tags` BEFORE `hasCustomUi`/`customUi` (`vendor/distingNT_API/include/distingnt/api.h:468`). Initializing `.tags = ...` after `.customUi = ...` is ill-formed and the compiler rejects it. The canonical `plugins/probes/aeabi_probe.cpp` factory doubles as the reference for field order when adding a new factory.

## ARM unresolved-symbol surface (firmware contract)

Firmware resolves at load: `NT_*` ABI (`NT_drawText`, `NT_screen`, `NT_jsonParse*`, `NT_intToString`), `_GLOBAL_OFFSET_TABLE_`, and newlib `memcpy`/`memset`/`memmove`/`strlen`/`strcmp`/`logf`/`powf`. NOT resolved: `__aeabi_d2lz` (handled by compiler_rt `fixdfdi.c` + `fixunsdfdi.c` + `fp_lib.h` + `int_math.h` from `vendor/llvm-project/compiler-rt/lib/builtins/` (submodule pinned at `llvmorg-19.1.0`), Apache-2.0; `fixdfdi.c` emits the EABI alias `__aeabi_d2lz` via its internal `AEABI_RTABI` macro when `__ARM_EABI__` is defined), `snprintf` AND `vsnprintf` (newlib-nano omits both; inline integer format. `shim/src/graphics.cpp` and `shim/src/host_proxy.cpp::format_u2` are the precedents for two-digit unsigned formatting without pulling either). `arm-none-eabi-nm <plugin.o> | grep ' U '` enumerates.

## Vendor dep cpps must link into ARM plug-in

Vendor non-header-only `.cpp` files (not just headers) must compile into the ARM plug-in `.o`. Per-dep host tests cover them, but ARM-side calls only resolve if the cpps are linked into the consuming applet's `.o`. Linkage is per-applet via `VENDOR_DEPS_<APPLET>` in the Makefile: e.g. `VENDOR_DEPS_LowerRenz` lists `build/arm/vendor_src/streams_{resources,lorenz_generator}.o`, compiled in place from `$(HEM_SRC_DIR)` by the `build/arm/vendor_src/%.o` rule and pulled into the `BUILD_PER_APPLET` rule for LowerRenz only. Applets that need no vendor cpp leave their `VENDOR_DEPS_*` empty. Symptom of missing link: `arm-none-eabi-nm` lists `_ZN7streams15LorenzGenerator4InitEh` or similar as unresolved.

## C++ COMDAT section merge in ARM partial-link

gcc emits one `.text._ZN<mangled>` COMDAT section per inline class method. `ld -r` preserves them under SHF_GROUP even with a `-T SECTIONS{}` script. Pipeline collapses them: `arm-none-eabi-objcopy --remove-section='.group'` followed by `ld -r -T shim/merge_sections.lds` merges all `.text._Z*`/`.data.rel.ro._Z*`/etc into single canonical sections. Drops ~1640 sections to ~14 with no behavior change. The section explosion is NOT a firmware cap (verified by deploying a 12-section 83KB build that still failed), but the cleaner artifact is easier to inspect and ship. Use `arm-none-eabi-readelf -W -S` (not `objdump -h`, which truncates) to inspect full mangled section names.

## Plugin directory layout (per-applet pilot release onward)

`plugins/applets/`, `plugins/hosts/`, `plugins/probes/` are the canonical
locations for per-applet plug-ins, host plug-ins, and diagnostic
probes. The top-level `applets/` directory was removed in the 2026-05-23
cleanup release. Probes (`aeabi_probe`, `bus_probe`, `section_probe`,
`solo_probe`, `reentrancy_probe`) live under `plugins/probes/`.

Per-applet plug-ins compile via the `BUILD_PER_APPLET` Makefile macro
and the `ALL_APPLET_LIST` variable (`PILOT_APPLET_LIST` is a
backwards-compat alias). Each per-applet `.o` is small (16-20 KB
`.text`), well under the firmware's ~82 KB per-`.o` cap. Hosts
(`Hemispheres_host.o`, `Quadrants_host.o`) compose per-applet plug-ins
at runtime through the firmware `_NT_slot` API plus the versioned
`HemiPluginInterface` function-pointer-in-data ABI.

## Adding a sparse-checkout vendor submodule

`vendor/llvm-project` (compiler-rt builtins) is added as a sparse
submodule. `.gitignore` ignores `vendor/*`, and a plain
`git submodule update` does NOT carry sparse-checkout config, so two
non-obvious steps are required (both codified in `bootstrap.sh`):

- `git add -f vendor/<name>` to stage the gitlink past the `vendor/*`
  ignore rule (the existing submodules were added before that rule).
- `git submodule absorbgitdirs vendor/<name>` only works AFTER the path
  is staged; run it after `git add -f`, not before.

`bootstrap.sh` carries an idempotent clone-vs-reapply block: on a fresh
checkout it clones `--no-checkout --depth=1 --filter=blob:none`, applies
`sparse-checkout set compiler-rt/lib/builtins`, checks out the tag, and
absorbs the git dir; on re-runs (including new worktrees) it runs
`sparse-checkout reapply`. `make vendor` calls `bootstrap.sh` so every
provisioning path applies the sparse config. Verify with
`git -C vendor/llvm-project sparse-checkout list` (and
`test ! -d vendor/llvm-project/llvm` confirms the full tree was not
pulled; the sparse tree is ~2 MB, not multi-GB).

## Markdown discipline

After editing any `.md` file, run `markdownlint <file>` and fix all errors. The repo's `.markdownlint.json` relaxes a small set of rules (long lines, HTML, sibling-only duplicate headings); the rest are enforced.

## Deployment

`make deploy` requires the NT to be in USB disk mode (Misc menu) and mounted (default `/Volumes/NT`). `make deploy-sysex` requires NT firmware v1.13+ for the plug-in rescan sysex, and the NT must be free of any other USB-MIDI client. See `docs/hardware-deploy.md` and `docs/nt-sysex-protocol.md` for the full procedure.

`make deploy-sysex` (`push_plugin_to_device.py`) talks sysex to the NT. The script auto-selects a free `disting NT` port and honors `NT_SYSEX_PORT` to force one. macOS CoreMIDI endpoints are shareable, so opening the port succeeds even while nt_helper is connected; the contention is protocol-level, not an OS lock. The upload is a request/ACK handshake on the IN port, and concurrent nt_helper sysex traffic can swallow the ACK and fail the upload ("Error uploading plug-in file!"). If the NT exposes a second USB-MIDI port, point deploy at a different one than nt_helper uses (via `NT_SYSEX_PORT` or autoselect) to isolate the two streams; with a single shared endpoint (the case on the current Mac), pause nt_helper activity or disconnect it for the upload. The script runs `newPreset()` before uploading, so a failed upload blanks the device preset. A sysex `0x7F` reboot (send via `mido`) reloads plug-in code and re-caches the SRAM requirement; the device auto-loads its last preset on boot. Uploads are occasionally flaky even with no other client: in a multi-`.o` batch an individual upload can fail while neighbors succeed; retry the failed `.o` after a short pause.

## Automated hardware smoke test with the Verifier plug-in

The Verifier plug-in (`plugins/probes/Verifier.cpp`, GUID `Vrfy`) closes the hardware-in-the-loop verification loop. It reads any bus and renders a screenshot-parseable readout (Numeric `sNN.fff` or Scope). The cable-free topology is: put the plug-in-under-test in an earlier slot and Verifier in a later slot of the same preset; the under-test writes its output bus, Verifier reads that same bus the same frame. Slot order is load-bearing. Verifier must run after the under-test or it reads the bus before it is written. Validated on hardware 2026-05-29 (issue #37, PR #62).

End-to-end procedure (the parts only a human can do are called out; everything else is `nt_helper` MCP plus shell):

1. Build the objects: `make build/arm/<UNDER_TEST>.o build/arm/Verifier.o`.
2. Deploy Verifier (and the under-test if not already installed) with `make deploy-sysex SYSEX_PLUGIN=build/arm/Verifier.o SYSEX_ID=0` (always `SYSEX_ID=0`). The script auto-selects a free `disting NT` port (`NT_SYSEX_PORT` overrides), so nt_helper can stay connected when a second NT port exists. With a single shared endpoint, pause nt_helper activity or disconnect it if the upload's ACK handshake fails.
3. Reboot to register. A brand-new GUID, or changed code for an existing GUID, does NOT reliably register from the `7A 08` rescan alone; it needs a reboot. Send the reboot sysex over the `disting NT` output port with `mido`: `F0 00 21 27 6D 00 7F F7`. A power cycle works too. The device auto-loads its last preset on boot. The human reconnects nt_helper after boot.
4. Refresh the nt_helper catalog. `nt_helper`'s `add` validates the GUID against its local metadata database, which lacks a freshly deployed custom plug-in until the human triggers nt_helper's algorithm sync/refresh in the app. Without the refresh, `add guid: "Vrfy"` fails with "not found in the list of available algorithms" even though the plug-in is registered and passing on the device (confirm via Misc > Plug-ins > View Info).
5. Build the preset through nt_helper so every slot stays nt_helper-editable: `new(name, [{guid: under_test}, {guid: "Vrfy"}])`. Batch `new` intermittently drops one algorithm ("did not appear"); if so, `add` the missing one singly with an explicit `slot_index` (insert the source at slot 0 to push Verifier to slot 1). nt_helper does NOT sync front-panel-added slots back into its editable model, so a slot added on the NT front panel reports "Slot N is empty" on `edit_parameter`. Only drive slots that nt_helper created.
6. Route the under-test output to the bus Verifier reads. Verifier defaults `First bus` and `Scope bus` to 13, which is `Output 1` in nt_helper bus naming (`Input 1..12` are buses 1..12, `Output 1` is bus 13). Set the under-test output enum to `Output 1` (often already the default).
7. Drive Verifier. Only enum params are settable over MCP: `View` (`Numeric`/`Scope`), `Mode` (`Mean`/`Min`/`Max`/`PkPk`), `Reset` (`Off`/`Reset`). The numeric params (`First bus`, `Count`, `Scope bus`, `Timebase`) are rejected by nt_helper `edit_parameter` ("requires a numeric value"); leave them at defaults or set them on the front panel. Enum strings must match exactly including any trailing pad spaces (the firmware-prepended `Bypass` enum is `"Off            "` / `"On             "`; Verifier's own enums are unpadded). Note the firmware prepends `Bypass` as parameter 0, so Verifier's `View` is parameter 1, `Reset` is parameter 5, etc.
8. Latch a reading: pulse `Reset` `Off` then `Reset` then `Off`. The transition into `Reset` clears the accumulators and re-arms the one-shot scope buffer in `parameterChanged`. The reading then latches independent of screenshot timing. `step` does not read `Reset`.
9. Screenshot. `mcp__nt_helper__show_screen` captures the FRONT-PANEL-FOCUSED slot, and there is no nt_helper focus tool, so the human must navigate to the Verifier slot on the device for the readout to be on screen. The Numeric view renders below the firmware title bar after the #61 fix (rows start at `y = kTitleBarH + row * kRowH`, `kTitleBarH = 12`). `show_screen` returns a PNG for visual reading.
10. Headless assertion. For pytest/CI rather than a visual check, capture raw pixels with `harness/scripts/nt_screenshot.py` `capture()` (direct sysex, no MCP) and parse with `harness/verifier/parser.py` `parse_numeric` / `parse_scope`. The font glyph templates in `harness/verifier/fixtures/font.json` are Verifier's own bitmaps, so the parser is hardware-valid with no font-capture step.

Accuracy reference: the Bus probe plug-in (`plugins/probes/bus_probe.cpp`, GUID `ProB`) sources a known DC of `Level% / 100` volts on its `Out` bus when `Out mode` is `Replace`. It is the calibration source for Verifier. Verified 2026-05-29: Level 0 reads `+00.000`, Level 50 reads `+00.500`, exact, confirming the bus ABI scale of 1.0f equals 1 V with zero offset at 1 mV resolution. Bus probe only spans 0 to +1 V; reference voltages beyond +1 V or negative need a different source (for example an LFO or offset algorithm).

## Merging PRs from a worktree

`gh pr merge` from inside a worktree fails when the default branch is checked out in the parent directory. Always pass `--repo danryan/nt_and_crime` and avoid `--delete-branch` (which triggers a local checkout). Use `gh pr merge <N> --squash --subject "..." --repo danryan/nt_and_crime`; delete the worktree and feature branch separately.

## Removing worktrees with submodules

`git worktree remove .worktrees/<name>` fails with "working trees containing submodules cannot be moved or removed". Always pass `--force` when removing a worktree that initialized submodules (every worktree in this repo does, since `make vendor` ran or submodules were init'd during provisioning).

## Hardware deploy: SRAM-size cache invalidation

Firmware caches `calculateRequirements` at plug-in scan time. Enlarging any `_NT_algorithm` subclass (adding a member, growing a buffer) requires a power cycle on the NT before the new build behaves correctly. Without it, the runtime allocates the prior smaller size, `construct()` overruns into adjacent memory, and downstream reads return garbage that often masquerades as expected behavior (e.g. a `step()` memcpy "restore" reading pixels indistinguishable from the firmware overlay it was supposed to overwrite). Power cycle re-reads the requirement; the build then works.

The plug-in rescan sysex (`0x7A 0x08`, what `make deploy-sysex` sends) re-registers `.text` but does NOT re-read `calculateRequirements`; only a reboot (`0x7F`) or a power cycle re-caches the SRAM requirement. So enlarging a struct and pushing it with `deploy-sysex` alone leaves the stale (smaller) requirement in place and the plug-in faults at add-algorithm time ("Failed to add algorithm", `construct()` overrunning the prior allocation). Reboot frame: `F0 00 21 27 6D <id> 7F F7` (no checksum), sent over `mido` to the `disting NT` port; the device auto-reloads its last preset on boot. This bites ACROSS apps when a shared header changes size: raising `kMaxSettings` in `plugins/apps/_per_app_runtime.h` grows `AppAlgorithm` (it embeds `parameters_storage[kMaxParams]` and `v_storage[kMaxParams]`, `kMaxParams = kIoParamCount + kMaxSettings`) for EVERY OC app, so a previously-scanned app regresses to "Failed to add" until one reboot re-caches all of them.

## Firmware footer overdraw pattern (Hemispheres + Quadrants hosts)

NT firmware paints its helper-text overlay onto `NT_screen` after the algorithm's `draw()` returns. `step()` runs at audio rate AFTER the firmware overlay pass and BEFORE the next display flush; writes to `NT_screen` from `step()` reach the display. Both composer hosts use this to suppress the overlay: `draw()` snapshots the bottom 16 rows into a per-instance `footer_cache[16*128]`; `step()` restores them while `steps_since_draw < kFooterRestoreSteps` (navigate-away guard, ~67 ms at 48 kHz/32-frame buffers). The counter is reset in `draw()` and incremented in `step()`. See `plugins/hosts/Hemispheres_host.cpp` `step_impl` + `draw_impl` tail. No `hasCustomUi` changes needed; softkey nav stays intact.

## HS:: globals are per-TU, not shared

Each per-applet ARM `.o` privately aggregates `shim/src/globals.cpp` via the `_per_applet_runtime.h` -> `hem_shim.h` -> `hem_shim_impl.h` include chain. Every plug-in therefore has its own copy of every `HS::*` global (`gfx_offset`, `gfx_clip_w`, `clock_m`, `popup_type`, ...). Writes from one TU do NOT reach reads from another TU. Implication: host plug-ins cannot drive per-applet rendering by writing `HS::*` from the host's TU; the write must happen in the same TU that reads it (e.g. `_per_applet_runtime.h::render_view_with_offset`). Hosts have no `globals.o` linked and `arm-none-eabi-nm` shows `HS::*` symbols as unresolved if the host references them.

## Per-applet standalone customUi mapping

Standalone per-applet plug-ins (loaded without a Hemispheres/Quadrants host) route firmware control events through `plugins/applets/_per_applet_runtime.h::route_custom_ui`. Current mapping: `kNT_encoderL` -> `on_encoder_turn` and `kNT_encoderButtonL` -> `on_button_press`. `kNT_button1` is intentionally NOT claimed in standalone; the applet's `on_aux_button` switches many vendor applets into param-edit mode and routing it to button 1 in standalone view ended up changing parameters unexpectedly. Hardware button 1 is therefore handled by the firmware default. Host plug-ins have their own routing in `plugins/hosts/*_host.cpp::customUi_impl` and forward button events to per-applet `on_aux_button` from there.

## Makefile prerequisite expansion timing

`$(VAR)` references in prerequisite lists expand at rule-parse time, not at rule-evaluation time. A `:=` variable defined AFTER its use in a prereq expands to empty. `SHIM_CORE_SRCS` was previously defined after its uses in `test_host_%`, `test_applet_%`, and `test_dep_%` rules; the empty expansion silently worked while no host TU referenced anything from the listed sources. Adding any new host reference to symbols defined in `shim/src/globals.cpp` / `shim/src/graphics.cpp` / `shim/src/icons.cpp` / `shim/src/quant/*` / `shim/src/cv_map/*` will surface the missing link. Fix: keep variable definitions ABOVE the first rule that uses them in a prereq.

## O_C full-screen apps foundation

Beyond the Hemisphere applets, the shim supports porting vendor O_C full-screen apps (the `OC::App` struct: `isr`/`loop`/`DrawMenu`/`DrawScreensaver`/`HandleButtonEvent`/`HandleEncoderEvent`/`HandleAppEvent`/`Save`/`Restore`) as independent NT plug-ins, one app per `.o` under `plugins/apps/`. The foundation landed on `dr/oc-apps-foundation` validated against Low-rents (`plugins/apps/Low_rents.cpp`) and Harrington 1200 (`plugins/apps/Harrington1200.cpp`); both `.text` are 11-15 KB, far under the cap. The design and the deviations discovered during the build are in `docs/superpowers/specs/2026-05-27-oc-apps-foundation-design.md` (see its "Implementation notes" section).

Shim surface: `shim/include/OC_apps.h` (App struct, `OC::apps` namespace), `OC_ui.h` (`UiControl` enum), `OC_ADC.h`/`OC_digital_inputs.h` (plus `DIGITAL_INPUT_*_MASK`)/extended `OC_DAC.h` (channel objects, output history, voltage scaling), `OC_menus.h` (hand-ported `menu::` widgets on `shim::Graphics`, plus `vectorscope_render`/`visualize_pitch_classes`), `OC_config.h`, extended `OC_strings.h`/`hem_graphics.h`. Vendor `OC_menus.h` cannot compile (it hard-includes the Teensy display driver), so the menu layer is hand-ported, not vendor-compiled.

Per-app runtime: `plugins/apps/_per_app_runtime.h` owns lifecycle dispatch, the control router state machine (edge detection, 500 ms long-press via `classify_release`, idle reset, `.mask`), the isr cadence accumulator (drives `isr()` at the vendor 16.666 kHz against the NT rate, increments `OC::CORE::ticks`, refreshes `OC::ADC` from and flushes `OC::DAC` to the routed buses), the per-row 32-byte centering shift (vendor `[0,128)` canvas centered to NT `[64,192)`), and the NT-parameter add-on (static `numParameters`, construct-time sentinel guard, `value_names + min` enum offset). `parameterChanged` and routing accessors read the firmware-managed `alg.v`, never the private `v_storage`.

Build model: a per-app TU `#define NT_OC_APP_TU 1`, which makes `_per_app_runtime.h` pull `shim/include/oc_shim_impl.h` (the OC aggregation header, `#include`-ing the shim `.cpp` bodies into the single TU, guarded by the shared `NT_HEM_NO_IMPL` sentinel; the OC analog of `hem_shim_impl.h`, it does NOT pull the Hemisphere-coupled headers). `BUILD_PER_OC_APP` (Makefile) mirrors `BUILD_PER_APPLET`; the host `test_oc_app_%` rule links the aggregating app `.cpp` without `SHIM_CORE_SRCS`. `OC_APP_LIST` enumerates the apps. To add an app: create `plugins/apps/<APP>.cpp` (copy the Low_rents/Harrington1200 pattern: aggregate, build the `OC::App` from the vendor `<PREFIX>_*` static thunks, the `_NT_factory` in vendor field order, and `.customUi = oc_runtime::dispatch_custom_ui_factory<map_long_press>`) plus `shim/include/oc_app_manifests/<APP>.h`, add it to `OC_APP_LIST` with any `VENDOR_DEPS_<APP>`, and a `harness/tests/test_oc_app_<APP>.cpp`.

customUI dispatch is consolidated in `plugins/apps/oc_customui_dispatch.h` (`oc_runtime::dispatch_custom_ui`): the runtime owns the whole customUI path (build the `::UI::Event`, edge loop, short/long classification, the `reinterpret_cast` to the forward-declared `OC::UI::Event`, the `NT_parameterOffset()` push-back). This is the ONLY file that pulls vendor `UI/ui_events.h`; core `_per_app_runtime.h` and `harness/include/oc_ui_sim.h` stay UI-free. A per-app `.cpp` carries no glue: it sets `.customUi = oc_runtime::dispatch_custom_ui_factory<map_long_press>` and test seams call `oc_runtime::emit_encoder` / `push_settings_to_params` directly. (#54/#63, PR #64.)

When porting a new O_C app, set the dispatch flag instead of copying glue: `dispatch_custom_ui_factory<true>` if the vendor app reads `EVENT_BUTTON_LONG_PRESS` (the runtime `LONG_RELEASE` maps to vendor `LONG_PRESS`; e.g. Harrington1200, FPART which reads long-press on DOWN and L), else `<false>` (Low_rents, BBGEN, BYTEBEATGEN).

The customUI dispatch path is hardware-verifiable only by physical encoder/button input; `nt_helper edit_parameter` routes through `parameterChanged`, not `customUi`, so it does NOT exercise dispatch. For a no-logic-change ARM refactor, `.text` is NOT byte-identical (the section-merge step relocates functions); gate on `objdump -d` mnemonic-histogram identity instead (normalize with `perl`; macOS BSD `sed` lacks `\s`).

Two firmware add-time hazards the runtime guards against (both root-caused on hardware; the host harness does not model them, so passing host tests and clean registration do NOT prove a plug-in will ADD): (1) `serialise()` must use `_NT_jsonStream::addNumber` only, never `addString`. The firmware calls `serialise` during add-algorithm, and `addString` faults there (surfaced as "Failed to add algorithm"); `oc_runtime::serialise` packs the `SettingsBase::Save()` blob four bytes per number under `oc_len` / `oc_w0..oc_wN`. (2) The isr cadence loop `while (numerator >= NT_globals.sampleRate)` is guarded with `if (sr == 0) return;` because the firmware runs `step()` at add before the sample rate is set (an unguarded loop spins forever). Always verify a new plug-in ADDS on hardware (`mcp__nt_helper__new` with its GUID), not just that it registers.

Three more on-device gotchas the runtime now handles (all root-caused on hardware, invisible to host tests): (1) the customUI settings push-back (`push_settings_to_params`) must add `NT_parameterOffset()` to the `NT_setParameterFromUi` index, which addresses the GLOBAL parameter table; `inst->v[base+s]` stays plug-in-relative. Omit it and the firmware re-applies the edit to the setting one row above. The sim's offset is 0, so a test must call `nt::set_parameter_offset(1)` to reproduce it. (2) `OC::DAC` values are 16-bit codes: `route_cv_output` maps `(code - 32768) / kCodesPerVolt` to +-5V (0V at the midpoint). Pitch apps reach it through `pitch_to_dac` (1V/oct); modulation apps write raw full-scale codes. Never reintroduce a pitch-only `/1536` output model; it rails full-scale modulation outputs. (3) The screensaver is timed off `OC::CORE::ticks` (25 s, vendor `SCREENSAVER_TIMEOUT_S`), not a draw count, and the firmware footer overlay is suppressed like the hosts (`draw()` caches the bottom 16 rows, `step()` restores them while `steps_since_draw < kFooterRestoreSteps`). Some `APP_*.h` (ENIGMA, MIDI, NeuralNetwork) inherit `HSApplication`, not `OC::App`, and need Teensy usbMIDI/EEPROM/`display.h`; they are out of scope for this foundation.

Two more lessons from the FPART port (`APP_FPART.h`, the first large-settings app, 109 settings). (1) Not every vendor setting can be an NT parameter. `_NT_parameter` min/max/def and `_NT_algorithm::v` are `int16_t` (max 32767). FPART's 99 chord settings are `STORAGE_TYPE_U32` with range up to 32323232, so they cannot be parameters without truncation and corruption. The port exposes only the 10 int16-safe head settings as NT parameters (pass that count, not `<X>_SETTING_LAST`, as `num_settings` to `oc_runtime::construct`); the U32 settings stay app-internal, edited through the customUI and persisted whole through the `Save/Restore` blob. The blob path is independent of `num_settings` (it calls the vendor `SettingsBase::Save`, which packs all settings), so persistence still covers the full table. A large-settings app's real shared blocker is therefore `oc_runtime::kMaxBlobBytes` (the `Save` blob must fit, or `serialise`/`deserialise` bail at the `storage_size() > kMaxBlobBytes` guard and silently drop persistence), not `kMaxSettings`. FPART's blob is 406 bytes; the cap was raised 256 -> 512. (2) A vendor app header may call free functions before their definitions, relying on the Arduino IDE's auto-prototype pass (`FPART_handleButtonEvent` calls `FPART_upButton` and five siblings defined further down `APP_FPART.h`). The shim build has no auto-prototype pass, so the per-app `.cpp` must forward-declare those functions before `#include`-ing the vendor header. The vendor source is not edited.

Three lessons from the BBGEN port (`APP_BBGEN.h`, the first quad-channel app: the app object is `QuadBouncingBalls`, four `BouncingBall` `SettingsBase` instances of 11 settings each). (1) Quad-channel apps do not fit the single-singleton `oc_runtime::construct(alg, app, settings, num_settings)` path. The runtime now offers `oc_runtime::construct_with_facade(alg, app, SettingsFacade, num_settings)` plus a `SettingsFacade::param_name(self, idx)` name-override hook (templated `construct` delegates to it; no behavior change for single-instance apps). The per-app `.cpp` builds a literal quad `SettingsFacade` with captureless lambdas (they reference the vendor file-scope singleton `bbgen` and the vendor `BBGEN_save/restore/storageSize` quad thunks, so no capture is needed) that dispatch logical row `idx` to `balls_[idx / SETTINGS_PER_CHANNEL]` setting `idx % SETTINGS_PER_CHANNEL`; `value_attr_at` returns `<Ball>::value_attr(idx % SETTINGS_PER_CHANNEL)` since all channels share one attr table. All `N_channels * settings_per_channel` settings become flat NT rows; `numParameters = kIoParamCount + N*S`. The NT param page has no channel grouping, so the rows carry channel-prefixed names ("A Gravity" .. "D Hard reset") built once at construct into a file-scope `char[N*S][16]` (the `_NT_parameter.name` pointer must outlive construct) and supplied through `param_name`. The vendor customUI keeps the A/B/C/D `menu::QuadTitleBar` selection UX intact. `plugins/apps/BBGEN.cpp` is the template for the next quad app (BYTEBEATGEN, 4x19). (2) `peaks_bouncing_balls.h` is header-only but reads `lut_gravity`, which is defined in `peaks_resources.cpp` (extern in the header). A "header-only DSP" vendor dep can still pull a resource `.cpp`: grep the header for `extern`/`lut_`/`Interpolate` usage and link the defining `.cpp` via `VENDOR_DEPS_<APP>` (BBGEN links `peaks_resources.o`; `nm` on the `.o` must show `lut_gravity` resolved, not `U`). (3) The shim surface for an app is not fully knowable from the spec's prereq audit; some gaps surface only at the first vendor-header compile. BBGEN needed four additive shim items the audit missed: the function-like `DIGITAL_INPUT_MASK(x)` macro (shim had only the named `DIGITAL_INPUT_n_MASK` constants), `OC::Strings::trigger_input_names` and `no_yes`, and `menu::QuadTitleBar` (`= TitleBar<kDefaultMenuStartX, 4, 6>`). Treat first-compile symbol errors as expected additive shim work, not port defects.

Three lessons from the BYTEBEATGEN port (`APP_BYTEBEATGEN.h`, the second quad-channel app: `QuadByteBeats` holding four `ByteBeat` `SettingsBase` instances of 19 settings each, reusing the BBGEN quad facade verbatim). (1) `kMaxSettings` is a real cap, not just headroom. BYTEBEATGEN flattens 4x19 = 76 NT parameter rows, the first app to exceed the prior `kMaxSettings = 64`. Raised to 80 in `_per_app_runtime.h`; the raise only enlarges the static per-instance `v_storage`/`parameters_storage` arrays, no behavior change for smaller apps. This is NOT the FPART subset situation: FPART capped its NT params because U32 chord settings overflow `int16_t`; every BYTEBEATGEN setting is U8/U4 (max 255, int16-safe), so the only blocker was the cap and raising it is the correct fix, not subsetting. The blob (`4 * ByteBeat::storageSize()` = 64 bytes) is unrelated to `kMaxSettings` and stays well under `kMaxBlobBytes = 512`. (2) Conditional vendor menus (`update_enabled_settings`/`num_enabled_settings`/`enabled_setting_at`, which hide the six loop settings when LOOP_MODE is off) map onto a flat, static NT parameter page by exposing ALL settings unconditionally; the runtime-varying enabled subset is a vendor customUI concern only and stays intact there. The flat NT page simply always shows the loop rows. (3) `SettingsBase::apply_value` returns false on a no-op (writing a setting's current value), so a conditional-menu test must force a real change to assert the apply return; assert the post-state (e.g. `num_enabled_settings()`), not the apply return for the default value. Additive shim work surfaced at first compile: `OC::Strings::bytebeat_equation_names` (16 equation labels) and a minimal `shim/include/util/util_misc.h` providing only `util::reverse_byte` (the screensaver calls it; the vendor `util_misc.h` drags `OC_options.h`, so the shim mirrors just the one inline). BYTEBEATGEN output is BIPOLAR full-scale modulation (`value = get_zero_offset(32768) + (int16_t)b`, -5V..+5V) and free-running (step_mode off advances every sample without a trigger), unlike BBGEN's gated unipolar envelopes. `plugins/apps/BYTEBEATGEN.cpp` is byte-for-byte the BBGEN template with renamed thunks and the 19-vs-11 counts.

Four lessons from the POLYLFO port (`APP_POLYLFO.h`, the first single-instance OC::App since the foundation apps, and the first to link the Frames quadrature-LFO engine; output is full-scale bipolar 4-channel modulation via `lfo.dac_code(0..3)`, free-running). (1) A standalone vendor host-dep `.cpp` that itself `#include`s vendor `util/util_math.h` defeats the include-guard-poison shadow. The poison works only when the shim header is pulled BEFORE the vendor sibling in a TU; but a vendor `.cpp` compiled as a `VENDOR_DEP_HOST_SRCS` source is its own TU root and its first include hits the vendor header directly (quote-include searches the vendor file's own dir first, which `-Ishim/include` cannot beat). Vendor `util/util_math.h` has ARM-only inline asm (`usat`, `umull`) that does not compile on the host. `frames_poly_lfo.cpp` is the first such case (the streams/peaks deps never include it). Fix: the `test_oc_app_%` Makefile rule now force-includes the shim shadow with `-include shim/include/util/util_math.h`, so the shim wins (poisons `UTIL_MATH_H_`) in every TU including the vendor `.cpp`. The shim `util/util_math.h` gained host-safe `multiply_u32xu32_rshift24` / `multiply_u32xu32_rshift` (64-bit-intermediate equivalents of the vendor `umull` versions) since suppressing the vendor body drops them and the Frames engine calls one. The ARM build is unaffected (the app TU poisons via normal ordering; the standalone `frames_poly_lfo.o` ARM compile sees the real vendor asm, valid on ARM). (2) `digitalReadFast(TR4)` (and `TR1..TR4`) is defined INLINE in the shim `OC_gpio.h`, delegating to `OC::DigitalInputs::read_immediate`, NOT via an out-of-line accessor in `shim/src/globals.cpp`. globals.cpp is in `SHIM_CORE_SRCS` (linked into every Hemisphere applet host test); putting the accessor there drags `oc_io::trigger` (OC-only, defined in `shim/src/oc/io.cpp`) into the applet links and fails them with an undefined symbol. Only OC-app TUs include `OC_gpio.h`, and they link `io.cpp`, so the inline keeps the `oc_io` reference confined to OC apps. `OC_gpio.h` includes `OC_digital_inputs.h` (self-contained, own guard; not a cycle). (3) The issue's forecast "VBiasManager stub" is a non-deliverable: the entire `VBiasManager` class and the app's `saveVbias`/`restoreVbias` are `#ifdef VOR`, and the shim does not define VOR, so `VBiasManager.h` reduces to its include guard plus `#include "OC_options.h"` (vendor `OC_options.h`, 87 lines, compiles clean with VOR undefined; no shim shadow needed). With VOR off, `POLYLFO_SETTING_VBIAS` is absent and `POLYLFO_SETTING_LAST == 21`; all 21 settings are int16-safe so all become NT rows (no FPART subsetting). The reusable learning for the later VOR-touching apps: VOR-conditional vendor code compiles to nothing without the define; do not write a stub for it. (4) Additive shim work surfaced at first compile (treat as expected, not defects): `OC::Strings::off_on` (`{"off","on"}`), `OC::bitmap_indicator_4x8` (4-byte freq-mult indicator), and the `OC_gpio.h` `TR4`/`digitalReadFast` from lesson 2. `plugins/apps/POLYLFO.cpp` is the single-instance template (Harrington1200 shape) for the next single-instance app; `VENDOR_DEPS_POLYLFO` links `frames_poly_lfo.o` + `frames_resources.o` (the wavetable LUTs `lut_increments_med` / `wt_lfo_waveforms`).

Four lessons from the AUTOMATONNETZ port (`APP_AUTOMATONNETZ.h`, a vector-sequencer that walks a 5x5 grid of `TransformCell` and drives a neo-Riemannian `TonnetzState`; single-instance `AutomatonnetzState` (itself a 7-setting `SettingsBase`) holding `TransformCell cells_[25]` of 4 settings each; output is 4-channel 1V/oct triad pitch, same tonnetz engine as Harrington1200). It is the first GRID-SUBSET app. (1) The grid-subset persistence gotcha is load-bearing: `oc_runtime::serialise/deserialise` persist through the SettingsFacade's `save`/`restore`/`storage_size`, which the default `make_facade<Settings>` binds to the facade `Settings::Save` (here `AutomatonnetzState::Save`, covering ONLY the 7 GRID settings). That is correct for the NT parameter rows, but the 25 cells live in a SEPARATE member array persisted by the `OC::App` Save thunk (`Automatonnetz_save` packs grid + all 25 cells), which the runtime never calls. So a naive subset port silently DROPS the entire grid on preset reload (host symptom: a cell value does not survive serialise/deserialise). Fix: build the standard facade with `make_facade(&singleton)` for get/set/value_attr, then OVERRIDE `facade.save`/`facade.restore`/`facade.storage_size` to captureless lambdas calling the full `Automatonnetz_save/restore/storageSize`, and pass it to `construct_with_facade`. Whenever a subset app keeps editable state outside the SettingsBase whose settings become NT params, the facade blob hooks MUST point at the full app Save/Restore or that state is not persisted. The full blob (7 + 25*4 bytes) stays far under `kMaxBlobBytes`. (2) O_C apps have no Arduino `random()`: it is a hem-only function-like macro (`HemisphereApplet.h` `#define random(...) hem_shim_random(...)`), and OC-app TUs never include that header. AUTOMATONNETZ (`ClearGrid` randomizes cell transforms) is the first OC app to call `random(howbig)`; the shim adds `long random(long)` / `random(long,long)` to `Arduino.h` guarded by `#ifndef random` (so a hem TU that pulls Arduino.h after the macro is defined skips them; the host libc `long random(void)` is an arity-distinct overload that coexists). Do NOT also add `randomSeed` to `Arduino.h`; `HemisphereApplet.h` already defines it and a hem TU includes both. (3) The customUI param-sync test MUST drive `p->factory->customUi` (which is `oc_runtime::dispatch_custom_ui`, the only path that emits `UI::Event`s to the vendor handlers); `oc_ui_sim::press`/`turn_encoder` call `oc_runtime::customUi` directly, which does runtime bookkeeping ONLY and never reaches the app, so an encoder edit through them changes nothing. For cursor-based editing apps (Automatonnetz's grid cursor starts non-editing), the test must first emit a short BUTTON_R press/release (the toggle) via `factory->customUi` before the ENCODER_R turn lands on a setting. H1200's encoder-L edits a setting directly, so its sync test needs no toggle; do not assume that shape. (4) Additive shim work at first compile (expected, mechanical): the `util/util_sync.h` shadow (vendor uses ARM CMSIS `__LDREXW`/`__STREXW`/`__DMB` + `<arm_math.h>`; shim provides a portable non-atomic `CriticalSection`/`TryLock`/`Lock` poisoning `UTIL_SYNC_H_`, correct because the shim has no preemptive ISR), `OC::ui` (a no-op `encoder_enable_acceleration`, inline in `OC_ui.h`), and `OC::circle_bitmap_8x8` (vendor `OC_bitmaps.cpp:67`). `util/util_grid.h` (the `CellGrid` walker) and the tonnetz headers are header-only and portable as-is (H1200 already proved tonnetz); the four enum-string tables are defined file-scope INSIDE the vendor app header, not `OC::Strings`, so no shim work. `VENDOR_DEPS_AUTOMATONNETZ` is empty. `plugins/apps/AUTOMATONNETZ.cpp` is the template for a single-instance grid-subset app.

Five lessons from the ENVGEN port (`APP_ENVGEN.h`, the vendor "Piqued / 4x EG": a quad app of four `peaks::MultistageEnvelope` channels with per-channel euclidean gating; `QuadEnvelopeGenerator envgen`, 33 settings/channel, gate-triggered unipolar envelopes). It reuses the BBGEN quad facade verbatim. (1) `kMaxSettings` does NOT affect the `.text` cap. ENVGEN flattens 4x33 = 132 NT rows, exceeding the prior `kMaxSettings = 80`; raised to 160. Empirically verified the raise leaves every app's `.text`/`.data`/`.bss` byte-identical (H1200 text=27344, POLYLFO text=29674 unchanged at 80 vs 160) because `kMaxSettings` only sizes the `AppAlgorithm` member arrays `parameters_storage[kMaxParams]` / `v_storage[kMaxParams]`, which are per-instance DATA counted in `req.sram` (the runtime SRAM pool, via `sizeof`), never code. So the ~82 KB scan-time `.text` cap is untouched by the raise; the only on-device consequence is the documented SRAM-cache reboot (deploy the enlarged build, then power-cycle once so previously-scanned apps re-cache `calculateRequirements`, else they fail to ADD). (2) The vendor enable-guard name is NOT mechanical: `APP_ENVGEN.h` is guarded by `ENABLE_APP_PIQUED` (the app's original name), not `ENABLE_APP_ENVGEN`. A wrong guard macro compiles silently (the `#ifdef` body is skipped) and surfaces only as "undeclared identifier ENVGEN_init" at the App-aggregate. Grep the vendor header's `#ifdef ENABLE_APP_*` before assuming the file's name. (3) A feature-rich app surfaces more additive shim symbols than the audit forecasts; treat them as expected build work, not defects, as long as each is a mechanical hand-port and the boundary is sound. ENVGEN needed, beyond the forecast euclidean draw: `OC::DigitalInputDisplay` (trigger-flash animator, vendor `OC_digital_inputs.h:148`), `OC_CORE_TIMER_RATE` (`OC_config.h:24`), `OC::bitmap_loop_markers_8` + `kBitmapLoopMarkerW` (`OC_bitmaps.cpp:58`), `menu::SettingsListItem::DrawValueMax` (`OC_menus.h:360`), and four `OC::Strings` tables. Enumerate the COMPLETE missing-symbol set up front with `g++ -ferror-limit=0 -fsyntax-only` on the per-app `.cpp` (the cascading "cannot initialize object parameter of type SettingsBase<...>" errors are a red herring; they clear once the missing member type, here `DigitalInputDisplay`, is added). (4) `menu::DrawMask<rtl,max_bits,height,padding>` (the euclidean-rhythm mask widget, both overloads) was dropped from the foundation shim `OC_menus.h`; re-added for ENVGEN and reusable by SEQ. The vendor `OC_euclidean_mask_draw.h` is header-only and compiles unmodified once DrawMask + `EuclideanPattern` (already shim-owned in `shim/src/cv_map/bjorklund.cpp`) are in scope; no vendor bjorklund `.cpp` dep. (5) `peaks_multistage_envelope.cpp` reads `lut_env_increments` from `peaks_resources.cpp`, the same resource BBGEN already links, so `VENDOR_DEPS_ENVGEN := peaks_multistage_envelope.o peaks_resources.o`. ENVGEN's default Amplitude setting is 127 (full), so a gated channel produces a non-zero envelope with no test setup. `plugins/apps/ENVGEN.cpp` is the BBGEN quad template with renamed thunks and the 33-vs-11 counts.

Five lessons from the PASSENCORE port (`APP_PASSENCORE.h`, a voice-leading chord sequencer: single-instance `PASSENCORE` `SettingsBase` of 18 settings plus a separate `PassencoreState` holding the scale-editor / menu-cursor customUI state; output is four-voice 1V/oct pitch via `play_chord` -> `OC::DAC::set_pitch`). It is the first MID-ARRAY setting-subset app and the first app to instantiate a vendor UI editor. (1) A subset whose excluded setting is NOT at the tail needs an index remap, not a prefix truncation. `PASSENCORE_SETTING_MASK` (index 1) is `STORAGE_TYPE_U16` (range 1..65535); `_NT_parameter`/`_NT_algorithm::v` are `int16_t` (max 32767), so the mask overflows and cannot be a parameter (the FPART U32 lesson), and it is a 12-bit scale mask edited via the scale-editor customUI anyway. Expose the other 17 by overriding `facade.get_value`/`apply_value`/`value_attr_at` with a captureless remap `phys = i >= kSkip ? i + 1 : i` (`kSkip = PASSENCORE_SETTING_MASK`, a compile-time constant, so the lambdas stay capture-free) and pass `num_settings = LAST - 1`. Unlike AUTOMATONNETZ, the excluded setting lives in the SAME `SettingsBase` whose `Save()` serialises all 18 values, so the default `make_facade` blob hooks persist the mask with no override; verify with a round-trip that mutates the mask through a `pc_set_mask` seam and asserts it survives. (2) A vendor UI editor template (`OC::ScaleEditor`, a `PassencoreState` member) lives in `namespace OC` and does member access on `UI::Event` / `UI::EVENT_*`. Inside `namespace OC`, bare `UI::` binds to the incomplete `OC::UI::Event` the foundation forward-declares in `OC_apps.h` (to keep core TUs UI-free), so the editor fails with "member access into incomplete type" and the vendor app cannot pass its `::UI::Event` into the editor. Fix backward-compatibly: `OC_apps.h` now `#ifdef UI_EVENTS_H_` aliases `namespace OC { namespace UI = ::UI; }` (so `OC::UI::Event` IS `::UI::Event`), else keeps the forward-decl; the per-app `.cpp` selects the alias by `#include "UI/ui_events.h"` BEFORE `_per_app_runtime.h`. Apps without a vendor editor include ui_events.h after the runtime and keep the forward-decl regime (their `reinterpret_cast` onto the App handler pointers degenerates to an identity cast), so no existing app regresses. PASSENCORE reads no `EVENT_BUTTON_LONG_PRESS`, so `dispatch_custom_ui_factory<false>`. (3) The shim `OC_scales.cpp` shipped a 1-entry `{nullptr}` `scale_names` stub; PASSENCORE uses `OC::scale_names[scale]` as the SCALE enum `value_names` and in its menu, which read out of bounds. Ported the full vendor 99-entry `scale_names` table verbatim into the shim (data, not a vendor edit; entry count equals `NUM_SCALES`); reused by the later scale apps (#48 DQ, #50 QQ, #51 CHORDS). `OC::DUMMY` is `OC_scale_edit.h:19` (`const bool`), no shim work. (4) `SettingsBase::apply_value` returns false on a no-op write (reaffirming the BYTEBEATGEN lesson); a settings round-trip must assert the post-state (`get == written`), not the apply return, because some written value may coincide with the current one. (5) Additive shim work surfaced at first compile from the editor headers the vendor app pulls UNCONDITIONALLY (`OC_scale_edit.h` and `OC_chords_edit.h` compile even with the chord editor commented out, because non-dependent names in their template bodies are looked up at definition time): `OC::Strings::{scale_degrees_maj,scale_degrees_min,accidentals,scale_id,channel_id,scaling_string,chord_property_names}` (globals.cpp, bytes verbatim from vendor `OC_strings.cpp`), `OC::UiOps::{read_immediate,SetButtonIgnoreMask,IgnoreButton,_preemptScreensaver}` (no-ops in `OC_ui.h`), `OC::DAC::set_scaling` (1V/oct collapse no-op), `menu::SettingsListItem::DrawNoValue` (`OC_menus.h`), and `OC::{bitmap_empty_frame4x8,bitmap_end_marker4x8}` (`OC_bitmaps.cpp:28,32`). `braids_quantizer` + `OC_scales` ride the OC shim-impl aggregation; only `OC_chords.cpp` + `OC_input_map.cpp` need `VENDOR_DEPS_PASSENCORE`. `plugins/apps/PASSENCORE.cpp` is the template for a single-instance app that exposes a non-contiguous setting subset and embeds a vendor UI editor.

Five lessons from the DQ port (`APP_DQ.h`, vendor "Meta-Q": two `DQ_QuantizerChannel` quantizer channels of 31 settings each, file-scope array `dq_quantizer_channels[2]`, each with four scale slots + a Turing-machine source; output is four-channel 1V/oct pitch, ch0 -> DAC A/C, ch1 -> DAC B/D). It is the first app to COMBINE the BBGEN quad facade (N = 2) with the PASSENCORE mid-array subset. (1) A multi-channel facade with a per-channel subset composes the two patterns: the facade dispatches a flat logical row to `channels[idx / per_channel]` (BBGEN) AND remaps the within-channel row past the excluded block (PASSENCORE). DQ's four U16 scale masks (`MASK1..MASK4`, contiguous physical indices 9..12, range 1..65535) overflow int16 and are edited via the scale editor, so they are excluded per channel: `phys_in_channel(w) = w < 9 ? w : w + 4`, `kExposedPerChannel = LAST - 4 = 27`, 2 * 27 = 54 flat rows. The masks persist because each channel's `SettingsBase::Save` covers all 31 settings and `DQ_save` loops both channels; override the facade `save`/`restore`/`storage_size` to the whole-app `DQ_save`/`DQ_restore` (the AUTOMATONNETZ blob-override pattern). (2) The PASSENCORE foundation carries straight over: the `OC::UI`-alias mechanism (DQ instantiates `OC::ScaleEditor<DQ_QuantizerChannel>`, so include `ui_events.h` before the runtime), the full `scale_names` table, and every `OC_scale_edit.h` symbol needed no new work. (3) The vendor enable-guard is `ENABLE_APP_METAQ`, not `ENABLE_APP_DQ` (reaffirming the ENVGEN non-mechanical-guard lesson; grep `#ifdef ENABLE_APP_*` in the header). (4) `DQ_QuantizerChannel` uses `util::TuringShiftRegister` WITHOUT the vendor header self-including `util/util_turing.h`; on the Arduino build some other TU pulls it first, but the shim per-app `.cpp` must `#include "util/util_turing.h"` itself (header-only, portable). The cascading "cannot initialize object parameter of type SettingsBase<...,31>" errors are the ENVGEN red herring; they clear once the missing member type (the Turing register) is in scope. (5) New header-only vendor deps `OC_visualfx.h` (`OC::vfx::ScrollingHistory`), `util/util_history.h`, and `extern/dspinst.h` (ARM-asm with a portable C `#else` fallback) compile as-is, no poison shadows; additive shim work was `OC::Strings::{cv_input_names,channel_trigger_sources,TM_aux_cv_destinations}` (globals.cpp), the `OCTAVES` macro (OC_config.h), `OC::DAC::{pitch_to_scaled_voltage_dac,get_octave_offset}` (1V/oct collapse, OC_DAC.h), and `menu::SettingsListItem::Draw_PW_Value` (OC_menus.h). `VENDOR_DEPS_DQ` is empty. Shim DAC-fidelity limit: the quantizer MAIN output goes through `pitch_to_scaled_voltage_dac` with a continuous-mode offset against the hardware's CALIBRATED DAC; the shim collapses to an uncalibrated 1V/oct `pitch_to_dac`, so the host main-channel code is not meaningful (it clamps to 0 while the aux channels settle at the 0V code). The host output test asserts only that the ISR -> quantizer -> DAC path runs (outputs not all zero); main-pitch fidelity is a hardware-smoke concern. `plugins/apps/DQ.cpp` is the template for a multi-channel app with a per-channel setting subset.

Five lessons from the ASR port (`APP_ASR.h`, the Analog Shift Register: a single-instance `ASRApp` SettingsBase of 27 settings clocking a four-stage quantizing shift register; output is four-channel 1V/oct pitch via `OC::DAC::set`). It is the PASSENCORE single-instance template plus the integer-sequence digit-table subsystem. (1) The ARM build does NOT force-include `shim/include/util/util_math.h` (only the `test_oc_app_%` host rule does, the POLYLFO fix). ASR is the first OC app to use `SlewedValue` / `USAT16` from that shadow, and `APP_ASR.h` does not transitively pull util_math.h before using them, so the per-app `.cpp` MUST `#include "util/util_math.h"` explicitly before `APP_ASR.h` or the ARM compile fails ("`SlewedValue` does not name a type") while the host passes. A host-green port is NOT ARM-proven; always `make build/arm/<APP>.o`. (2) `__popcountdi2` (the 64-bit popcount compiler-rt builtin) is odr-used by ASR and is NOT in the firmware's resolved symbol set NOR the prior `COMPILER_RT_OBJS` list; add `popcountdi2.c` to `COMPILER_RT_SRCS`. `arm-none-eabi-nm <app>.o | grep ' U '` after the ARM build is the gate (it showed `U __popcountdi2`). The addition is partial-linked into every plug-in and is benign (tiny, no duplicate). (3) Subset apps that pull a vendor source-data subsystem must hand-transcribe the data into the shim because the vendor definition lives in the unlinkable `OC_strings.cpp` (linking it duplicates note_names etc.). ASR's `IntegerSequence` odr-uses eight `uint8_t[128]` digit tables (`pi_digits`, `van_eck`, `sum_of_squares_of_digits_of_n`, `digsum_of_n`, `digsum_of_n_base4`, `digsum_of_n_base5`, `count_down_by_2`, `interspersion_of_A163253`); these plus the full `scale_names_short` (a 1-entry `{nullptr}` stub before, used by ASR's SCALE enum) were ported verbatim into globals.cpp / OC_scales.cpp. Token-diff every transcribed data table against vendor (`uint8_t[N]` initializers silently zero-pad on under-count, so a hand-count is not enough). (4) `randomSeed` is defined in the shim `HemisphereApplet.h` (an inline function, not a macro, so `#ifndef` cannot dodge it) and a hem TU includes both it and `Arduino.h`. ASR/SEQ (OC apps) need `randomSeed` but never include HemisphereApplet.h, so add it to `Arduino.h` guarded by `#ifdef NT_OC_APP_TU` (defined only in OC-app TUs): the two definitions then live in disjoint TUs. It seeds the same `shim_detail` LCG `random()` draws from. (5) For a cursor-edit param-sync test, probe `enabled_setting_at` to find the cursor start position rather than assuming it is setting 0: ASR's scrollable cursor list starts at ROOT (SCALE/OCTAVE are left-encoder-steered and excluded from the right-encoder cursor; one scroll lands on the MASK row, where BUTTON_R opens the scale editor instead of toggling editing). The direction-2 test toggles editing at the start position and edits ROOT. Additive shim work: `SlewedValue` + `Atten` (shared `ATTEN_DEFINED` sentinel with CVInputMap.h), `OC::bitmap_hold_indicator_4x8`, `kIntSeqLen`, `OC::Strings::{mult,integer_sequence_names,integer_sequence_dirs}`. `VENDOR_DEPS_ASR` links `peaks_bytebeat.o` (the same vendor `.cpp` BYTEBEATGEN links). `plugins/apps/ASR.cpp` reuses the PASSENCORE template; the int-seq tables and `scale_names_short` it added are reused by QQ/CHORDS/SEQ.

Four lessons from the QQ port (`APP_QQ.h`, vendor "Quantermain": four `QuantizerChannel` instances of 51 settings each in the file-scope array `quantizer_channels[4]`, each quantizing a CV/Turing/logistic/bytebeat/int-seq source to a 1V/oct pitch output; the DQ quad-facade at N=4). It is the largest OC app so far (200 NT rows) and the first to hit the vendor-edit exception. (1) The vendor-HSIOFrame collision is unfixable by shadowing, and is the reason `vendor-patches/` exists. `APP_QQ.h` (and `APP_SEQ.h`) `#include "HSIOFrame.h"`, a Hemisphere header, but use nothing from it (grep for `HS::`/`IOFrame`/`frame.` is empty). The include is fatal in an OC-app TU: the include-guard-poison technique fails because vendor `HSIOFrame.h` and `HSUtils.h` are `#pragma once` (no named guard to define), and because the quote-include sits in the SAME dir as the hem headers it resolves to the VENDOR copies (the shim bare-name shadows that hem applet TUs get for free only win when the includer is in a sibling dir). Even after poisoning the named guards the vendor chain DOES check (`OC_CORE_H_`/`OC_GPIO_H_`/`OC_ADC_H_`/`OC_CONFIG_H_`), the vendor `HSUtils.h`/`PhzIcons` bodies still run and redefine the same symbols the shim's own `HSUtils.h` shadow (pulled by every OC TU via `globals.cpp`) already defines: `HEM_SIDE`, `QuantEngine`, `PackLocation`, `calibr8`, irreconcilable. Two full copies of one symbol set in one TU is unsuppressable by adding more includes. The fix is the provisioning patch (see "Vendor provisioning patches"): strip the dead `#include "HSIOFrame.h"` line from `APP_QQ.h`/`APP_SEQ.h` at bootstrap time. Reach for shadowing first; a provisioning patch is the last resort, only after proving shadowing structurally cannot win. (2) `kMaxSettings` raised 160 -> 208 (QQ flattens 4x50 = 200 rows; the U16 `CHANNEL_SETTING_MASK` at index 2 is excluded per channel, the single-mask variant of DQ's four-mask skip, so `phys_in_channel(w) = w < 2 ? w : w + 1`, `kExposedPerChannel = LAST - 1 = 50`). DATA-only growth (per-instance arrays in `req.sram`), `.text` untouched; the on-device cost is the documented SRAM-cache reboot. The four channels live in a separate array (not one SettingsBase), so the facade `save`/`restore`/`storage_size` MUST point at the whole-app `QQ_save`/`QQ_restore` (the AUTOMATONNETZ/DQ blob-override), or only one channel persists. (3) `multiply_u32xu32_rshift32` is defined by vendor `extern/dspinst.h` (only that variant, not rshift24/rshift). Apps that include dspinst (ASR, DQ) get it there; adding it to the shared shim `util/util_math.h` redefines it for them regardless of include order. QQ does NOT pull dspinst but its logistic-map/Turing scaling calls rshift32, so QQ defines it locally in its own `.cpp` (a `static inline`), and the shim `util_math.h` deliberately omits it with a comment. (4) The output-smoke test must drive triggers: unlike DQ ch0 (continuous default), every QQ channel quantizes on a hardware trigger (TR1..TR4, default buses 5..8), and all four outputs are MAIN pitch (no aux), so without a clock pulse nothing writes and all outputs read literal 0; a fired channel writes code 32768 (0V) which is non-zero. Drive the four trigger buses high plus CV, then assert "not all zero" (the path runs; main-pitch fidelity through the calibrated DAC is hardware-smoke, the DQ caveat). Additive shim work: `OC::Strings::octave_constraint` (globals.cpp, vendor `OC_strings.cpp:164`) and the local rshift32. `VENDOR_DEPS_QQ` links `peaks_bytebeat.o`. `plugins/apps/QQ.cpp` is the DQ quad template at N=4 with a single-mask skip.

Three lessons from the CHORDS port (`APP_CHORDS.h`, vendor "Chords": a four-voice chord sequencer. A scale + 12-bit active-note mask quantizes a root, then a user-defined chord progression voices it as a four-note chord on the four DAC channels; single-instance `chords` SettingsBase of `CHORDS_SETTING_LAST == 31` settings, plus a separate `chords_state` (ChordQuantizer) holding the customUI state). It is the PASSENCORE single-instance mid-array-subset template with a SECOND vendor editor and is HSIOFrame-clean (no provisioning patch). (1) A vendor app with multiple UI editors confirms the PASSENCORE `OC::UI` alias path scales: CHORDS instantiates both `OC::ScaleEditor<Chords>` and `OC::ChordEditor<Chords>`, and both editors plus the app `handleButtonEvent` read `EVENT_BUTTON_LONG_PRESS`, so it is `dispatch_custom_ui_factory<true>` (PASSENCORE was `<false>`: its app handler read no long-press even though its scale editor's body does). Include `UI/ui_events.h` before the runtime exactly as PASSENCORE/DQ do; nothing new. The mid-array U16 mask (`CHORDS_SETTING_MASK`, index 3, range 1..65535) is excluded with the same captureless remap (`phys_setting(i) = i >= 3 ? i + 1 : i`), 30 of 31 rows exposed, and because the mask lives INSIDE the `chords` SettingsBase the DEFAULT `make_facade(&chords)` blob persists it (no whole-app override; this is PASSENCORE, not the AUTOMATONNETZ/DQ/QQ separate-array case). (2) Re-adding a dropped chord widget pulls the vendor chord-presets data, and `OC_chords.h` / `OC_chords_presets.h` are MUTUALLY recursive. `menu::DrawChord` / `menu::DrawMiniChord` (vendor `OC_menus.h:151,178`) were dropped from the foundation shim `OC_menus.h`; CHORDS uses both. They read `OC::Chord` / `OC::user_chords` / `OC::qualities` / `OC::voicing`, so the shim shadow must pull that data, but `#include "OC_chords_presets.h"` FAILS standalone: presets.h includes `OC_chords.h` (line 5) whose `GetChord` signature uses `Chord` (line 92) before presets.h defines `struct Chord` (line 10). Include `OC_chords.h` FIRST instead: its guard wins, it pulls presets.h which defines `struct Chord` before control returns to `GetChord`. The widgets are `inline` and uncalled in non-chord apps, so `user_chords` is never odr-used there (zero link cost); only chord apps link `OC_chords.o`. Verified by ARM-building all 14 OC apps after the shared `OC_menus.h` touch. (3) `CHORDS_SETTING_LAST` is 31, not 30 (the enum tail is SCALE..NUM_CHORDS_CV then DUMMY + MORE_DUMMY, two unused 0..0 tail rows that stay exposed as harmless single-value params). Always read the enum's actual `_LAST`; a hardcoded count in the factory/registration test is the catch (the first host run failed `31 == 30`). The cursor's enabled list starts on MASK (`enabled_settings_[0]`, where BUTTON_R opens the scale editor), so the param-sync direction-2 test scrolls one step (ENCODER_R while not editing) to land on ROOT before toggling editing, unlike PASSENCORE whose cursor-0 SCALE toggles directly. Additive shim work: `OC::Strings::seq_directions` (globals.cpp, vendor `OC_strings.cpp:57`) plus the two menu widgets. `VENDOR_DEPS_CHORDS` = `OC_chords.o` + `OC_input_map.o` (same as PASSENCORE). Documented fidelity limit: the user chord-slot definitions live in `OC::user_chords[]`, not the `chords` SettingsBase, and the vendor app's own blob (`CHORDS_save == chords.Save`) does not serialise them either (global EEPROM page on hardware), so the NT port matches the vendor blob exactly and chord-slot edits do not survive a preset reload. `plugins/apps/CHORDS.cpp` is the template for a single-instance app that embeds two vendor editors and draws chord shapes.

## Vendor provisioning patches

Standing rule: vendor source is not hand-edited and is never committed-modified inside a submodule (the pin SHA never changes). The shim's include-guard-poison and bare-name shadowing cover almost every vendor-compat need without touching vendor source, and they remain the default. `vendor-patches/` is the ONE documented exception: tiny unified-diff patches applied by `bootstrap.sh` to the vendor working tree at provision time, never committed into the submodule. The pin SHA is unchanged; `git -C vendor/O_C-Phazerville status` simply shows the patched lines as a dirty working tree, which is expected.

When this is the right tool (and only then): the vendor edit is the removal of dead code that the shim CANNOT neutralize by shadowing. The first and currently only patch, `0001-oc-apps-drop-vestigial-hsioframe.patch`, removes the vestigial `#include "HSIOFrame.h"` from `APP_QQ.h` and `APP_SEQ.h` (see the QQ lessons above for why shadowing structurally cannot win: `#pragma once` headers resolved same-dir to the vendor copies, colliding with the shim's own `HSUtils.h` shadow that every OC TU pulls). Do NOT use a provisioning patch to add behavior, change logic, or paper over a shadowing gap you have not yet tried to close. Removing a no-op include that breaks an unrelated TU is the bar.

Mechanism (idempotent, in `bootstrap.sh` after the `O_C-Phazerville` submodule update): for each `vendor-patches/*.patch`, `git -C vendor/O_C-Phazerville apply --reverse --check` detects an already-applied patch and skips; otherwise `apply --check` then `apply`; a clean-application failure prints a WARNING (the pin SHA moved and the patch needs regenerating). `make vendor` calls `bootstrap.sh`, so every provisioning path (fresh clone, new worktree, re-run) applies it. To add a patch: make the edit in the vendor working tree, `git -C vendor/O_C-Phazerville diff -- <files> > vendor-patches/NNNN-<slug>.patch`, leave the edit in place for the local build, and document the why here.

## Shadowing a vendor header quote-included from inside another vendor header

The established bare-name `-Ishim/include` shadowing works only when the app TU includes the vendor header by bare name (the TU's own directory is `plugins/`, not the vendor tree). When a VENDOR header quote-includes another vendor header (for example `OC_trigger_delays.h` includes `"OC_digital_inputs.h"`, or `APP_H1200.h` includes `"OC_bitmaps.h"`), the compiler searches the including vendor file's directory first and finds the vendor sibling, which `-Ishim/include` cannot override. The fix is include-guard poison: the shim shadow defines the vendor header's own `_H_` include guard (for example `OC_DIGITAL_INPUTS_H_`, `OC_BITMAPS_H_`, `OC_STRINGS_H_`, `UTIL_MATH_H_`), so when the vendor sibling is later pulled it self-suppresses. The shim shadow must then provide every symbol the suppressed vendor body would have. This is how the O_C app foundation shadows `OC_digital_inputs.h`, `OC_bitmaps.h`, `OC_strings.h`, and `util/util_math.h`. The FPART port (`APP_FPART.h`, which quote-includes `OC_apps.h`, `OC_menus.h`, `OC_DAC.h`, `OC_ADC.h`, `OC_strings.h`, `OC_digital_inputs.h` from inside the vendor tree) extended the set: the shim `OC_menus.h`, `OC_apps.h`, `OC_DAC.h`, and `OC_ADC.h` now poison `OC_MENUS_H`, `OC_APP_H_`, `OC_DAC_H_`, and `OC_ADC_H_`. The `OC_menus.h` poison is load-bearing: the vendor `OC_menus.h` hard-includes the Teensy display driver plus vendor `OC_DAC`/`OC_config`/`OC_gpio`, so leaving it unsuppressed pulls the whole non-portable chain.
