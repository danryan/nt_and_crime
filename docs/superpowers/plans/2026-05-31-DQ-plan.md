# Plan: DQ (Dual Quantizer) NT plug-in

Date: 2026-05-31
Status: accepted
Vendor SHA: 7800d929f25868f9a8b7d3d50514532ee001649b
Issue: #48

Single unit, single implementer, on the shared branch `dr/oc-apps-port-2`. No
parallel fan-out. Sequenced because the steps share files (DQ.cpp, the Makefile,
the shared shim headers).

## Worklist

1. Shim Strings: add `cv_input_names`, `channel_trigger_sources`,
   `TM_aux_cv_destinations` (declare in OC_strings.h, define in globals.cpp).
2. Shim symbols: `OCTAVES` (OC_config.h); `OC::DAC::pitch_to_scaled_voltage_dac`
   and `get_octave_offset` (OC_DAC.h); `menu::Draw_PW_Value` (OC_menus.h).
3. Manifest: `shim/include/oc_app_manifests/DQ.h` (GUID `OCDQ`).
4. App: `plugins/apps/DQ.cpp` per the spec (dual facade, per-channel mask skip,
   `dispatch_custom_ui_factory<true>`, `ui_events.h` first, `MENU_REDRAW`,
   `ENABLE_APP_METAQ`, `util/util_turing.h` include, test seams).
5. Makefile: add `DQ` to `OC_APP_LIST`; empty `VENDOR_DEPS_DQ` /
   `VENDOR_DEP_HOST_SRCS_DQ`.
6. Test: `harness/tests/test_oc_app_DQ.cpp` per the spec test plan. Build, resolve
   first-compile shim gaps (`g++ -ferror-limit=0` to enumerate), get green.
7. ARM: `make build/arm/DQ.o`; confirm `.text` under cap and `nm` clean.
8. Regression: `make test-oc-apps-all` green; a hem applet (touches shared
   globals/OC_DAC/OC_menus) still builds and passes.
9. Docs: add a DQ lessons paragraph to CLAUDE.md; `markdownlint` the three docs
   and CLAUDE.md.
10. Commit on `dr/oc-apps-port-2` (conventional, scope `apps`).

## Worktree-dispatch checklist

Not applicable: single implementer in the current worktree on
`dr/oc-apps-port-2`. Submodules initialized. No subagent fan-out.

## Abort budget

- Step 6: more than 2 unforeseen poison shadows -> halt, reassess boundary.
- Step 7: `.text` over ~82 KB -> halt.
- Step 8: any other OC app or hem applet regresses from the shared-shim changes
  -> halt and fix before proceeding.
