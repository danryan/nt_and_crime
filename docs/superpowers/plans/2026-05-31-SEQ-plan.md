# Plan: SEQ (Sequins) NT plug-in

Date: 2026-05-31
Status: implemented
Vendor SHA: 7800d929f25868f9a8b7d3d50514532ee001649b
Issue: #52

Single unit, single implementer, on the shared branch `dr/oc-apps-port-2`. No
parallel fan-out. Sequenced (shared shim files: `OC_ui.h`, `OC_ADC.h`,
`OC_menus.h`, `OC_strings.h`, `hem_graphics.h`, `globals.cpp`, `graphics.cpp`).

## Worklist

1. Manifest `SEQ.h` (GUID `OCSQ`; 4 CV in / 4 CV out / 4 trig in, DQ I/O shape).
2. App `SEQ.cpp` (DQ multi-channel template; `ui_events.h` first; `util_math.h`
   before vendor header; skip mask block 10..14; whole-app blob override;
   `dispatch_custom_ui_factory<true>`; test seams).
3. Makefile: `OC_APP_LIST += SEQ`; deps `OC_patterns.o` + `OC_input_map.o` +
   `peaks_multistage_envelope.o` + `peaks_resources.o` (host + ARM).
4. Test `test_oc_app_SEQ.cpp`. Enumerate first-compile gaps with
   `g++ -ferror-limit=0`; resolve (no vendor edit); get host green.
5. Shim additions: `OC_UI_H_` poison (OC_ui.h); `seq_playmodes` (OC_strings.h +
   globals.cpp); `smoothed_raw_value` (OC_ADC.h); `Draw_PW_Value_Char`
   (OC_menus.h); `print(int, unsigned)` (hem_graphics.h + graphics.cpp).
6. ARM: `make build/arm/SEQ.o`; `.text` under cap and `nm` clean.
7. Regression: full OC-app suite green; every OC app ARM-builds (shared shim
   touches); one hem applet host + ARM still green (shared globals.cpp /
   graphics.cpp).
8. Docs: SEQ lessons paragraph in CLAUDE.md; `markdownlint` the docs + CLAUDE.md.
9. Commit on `dr/oc-apps-port-2`.

## Abort budget

- Step 4: more than 2 unforeseen poison shadows beyond OC_ui -> halt.
- Step 4: a vendor edit beyond the existing HSIOFrame provisioning patch needed
  -> halt.
- Step 6: `.text` over ~82 KB -> halt.
- Step 7: any other OC app or hem applet regresses -> halt and fix.

## Outcome

Shipped. Host 7/7 (103 assertions). ARM `.text` 48190 B, `nm` clean. Full OC-app
host suite (15 binaries) PASS; all 15 OC apps ARM-build clean; Cumulus hem applet
host (26 assertions) + ARM clean.

No new vendor patch (the existing HSIOFrame patch already covers `APP_SEQ.h`). The
DQ multi-channel template carried over: N = 2, five contiguous U16 masks (10..14)
excluded per channel (53 exposed/channel, 106 flat rows), whole-app blob override
because the channels are a separate array. Two vendor editors (pattern + scale),
`<true>` long-press dispatch. The single structurally-interesting shim addition
was the `OC_UI_H_` include-guard poison (SEQ is the first OC app whose vendor
header pulls vendor `OC_ui.h`); the other four (`seq_playmodes`,
`smoothed_raw_value`, `Draw_PW_Value_Char`, `print(int, unsigned)`) were
mechanical hand-ports. `kMaxSettings` (208) already covered 106 rows, no raise.
