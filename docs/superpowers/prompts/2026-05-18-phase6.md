# Phase 6: Hemisphere applet ports against Phase 5-shipped dependencies (autonomous)

Phase 5 shipped clean at squash commit `8f42965` with all 6 vendor-dep ports plus the time-injection helper plus Layer 0 shared infrastructure. Hardware smoke check green on three existing applets (Binary or Switch, ShiftGate or Voltage, ClockSkip or RndWalk). The shim now exposes the surface needed to port the bulk of remaining Hemisphere applets: VectorOscillator + WaveformManager + RelabiManager (dep-vec-osc), LorenzGeneratorManager + streams_lorenz_generator (dep-lorenz), tideslite + PhaseExtractor (dep-tideslite), full ClockManager with `step()` prologue hook (dep-clock-mgr), Quantizer subsystem + `HS::Quantize` pool on `HemisphereApplet` base (dep-quant), CVInputMap (dep-cv-map), and `step_n_inner_ticks` test helper.

This prompt ports approximately 25 confirmed Hemisphere applets in a single fan-out across three test recipe classes. Three vec-osc applets (Drum1, Drum2, ASR-Drums) are deferred for re-audit. The long-term clock-aware set (ClockSetup variants and open-ended clock-aware applets) is parked for Phase 7+.

This is an applet port phase, not a shim infrastructure phase. Layer 0 work is bounded: section markers + pack helper skeletons + pre-commit hook update + Phase 5 baseline verification. No new shim subsystems land; the recipes in this prompt assume Phase 5's shipped surface.

## Phase 6 scope

Ship the confirmed in-scope applet inventory in a single Layer 1 parallel fan-out. Inventory commits to its final list in the brainstorm; the candidate set below is the audit starting point.

**Class A (vendor-dep, ~9 applets):**
- dep-vec-osc: VectorLFO, VectorEG, VectorMod, VectorMorph, Relabi
- dep-lorenz: LowerRenz
- dep-tideslite: EbbAndLfo, WTVCO
- dep-cv-map: Combin8

**Class B (quantizer, ~14 applets):**
- Pigeons, Strum, Shredder, Carpeggio, Squanch, Chordinator, DualQuant, EnigmaJr, OffsetQuant, MultiScale, ScaleDuet, DuoTET, EnsOscKey, Calibr8

**Class C (helper-using, ~4 applets):**
- ResetClock (the natural first cat-C probe; exercises both Clock-driven state evolution and `OC::CORE::ticks` advancement)
- Shuffle, Xfader, Scope

**Class D (clock-mgr immediate, ~1 applet):**
- Metronome

**Explicitly deferred to Phase 7+:**
- Drum1, Drum2, ASR-Drums (vec-osc family; need re-audit against shipped dep surface)
- ClockSetup variants and the open-ended clock-aware applet set (long-term; Phase 7+ enumerates)

The brainstorm reconciles the candidate inventory against vendor headers at the current Phase 5 surface and commits to the final list. The math discrepancy between "28 named candidates" and "25 confirmed in-scope" is resolved in the brainstorm; document the reconciliation explicitly.

## The three most important rules

These three rules are repeated because Phase 3 attempt 1 and Phase 4 audit traced specific failures to violating them:

1. PARALLELIZE INDEPENDENT WORK. Default to parallel. Class A, Class B, Class C, and Class D applets are independent at the file-surface level (each owns a `.cpp` test region, no cross-applet state). The plan must explicitly declare which tasks are independent and dispatch them in a single parallel batch (multiple `Agent` tool calls in one message). End-to-end wallclock target is roughly the time of the slowest single applet plus Layer 0 prep plus integration.

2. EVERY IMPLEMENTER WORKTREE BRANCHES FROM `dr/phase6-applets-plan`, NOT FROM `main`. The parent agent must specify the base branch explicitly: `git worktree add <path> -b <implementer-branch> dr/phase6-applets-plan`. Never rely on the default.

3. NO SHIM SUBSYSTEM CHANGES. Phase 6 does not modify Phase 5 dep ports, the time-injection helper API, the ClockManager step() hook, or any compiler-rt files. If an implementer surfaces a missing dep surface, abort the task and surface to the parent agent. The deps shipped in Phase 5 are frozen; Phase 6 ports applets against them.

## Model selection

- Parent orchestrator: `opus` (this is what you are).
- Implementer subagents: `sonnet`.
- Audit/Explore subagents: `sonnet`.

## Autonomous execution contract

The agent runs through the following phases without pausing for user review except at the preflight checkpoint:

1. Audit + verification (read primary references, verify Phase 5 baseline, audit each candidate applet's vendor file surface, design Layer 0 deliverables, validate class assignments).
2. Preflight checkpoint (post a one-paragraph preflight report, proceed unless an abort condition fired during audit).
3. Phase 6 brainstorm (categorize, select, commit to final inventory).
4. Phase 6 spec (per-applet entries with class, recipe class, file:line citations).
5. Phase 6 plan (DAG structure, parallel dispatch, per-class abort thresholds).
6. Layer 0 (parent, sequential): section markers in test_hemispheres.cpp and applet_test_helpers.{h,cpp}, pre-commit hook update, ResetClock spec inheritance from Phase 4 abort report.
7. Layer 0.5 (parent, sequential): per-applet pack helper forward declarations (skeletons only; bodies land in Layer 1).
8. Layer 1 (parallel implementer subagents): all in-scope applets dispatched in a single message. Implementer count: ~25 unless the brainstorm trims.
9. Layer 2: integration on feature branch.
10. Layer 3: final verification (full regression: every existing applet test still passes, `make arm` clean with the 3 accepted cxx_runtime_stubs warnings, hardware smoke check on Phase 5 trio + one Phase 6 newly-landed pair).
11. PR open with Load-bearing decisions section.

The agent surfaces to the user only at preflight or when an abort condition fires.

## Branch context

Phase 6 runs on the NEW feature branch `dr/phase6-applets-plan` provisioned from `main` at `8f42965` (Phase 5 squash). The brainstorm confirms the parent worktree exists at `.worktrees/phase6-applets-plan`.

Initialize submodules in the new worktree: `git -C .worktrees/phase6-applets-plan submodule update --init --recursive --depth=1`.

## Worktree naming

- Parent worktree: `.worktrees/phase6-applets-plan` on branch `dr/phase6-applets-plan`.
- Implementer worktrees: `.worktrees/phase6-port-<slug>` on branch `phase6-port/<slug>`.
- Layer 0, Layer 0.5, Layer 2 commits land directly on `dr/phase6-applets-plan`.
- Implementer worktrees branch from `dr/phase6-applets-plan` at its head AFTER Layer 0 + Layer 0.5 commit. Parent does not dispatch any Layer 1 implementer until section markers and skeletons land and `make test-applets` + `make arm` pass on `dr/phase6-applets-plan`.

## Pre-commit hook

Inherit Phase 5's hook template. Update branch-name patterns to accept commits on:

- `dr/phase6-applets-plan` (Layer 0, Layer 0.5, Layer 2).
- `phase6-port/*` (Layer 1, forbidden surface enforced per-applet).

Reject commits on `main`, on any branch not derived from `dr/phase6-applets-plan`, and any `phase6-port/*` commit that stages a file outside the applet's allowed surface. The allowed surface for each implementer is bounded:

- `harness/tests/test_hemispheres.cpp` (only within the applet's BEGIN/END section markers).
- `harness/tests/applet_test_helpers.h` and `applet_test_helpers.cpp` (only the applet's pack helper section, if any).

Hard-reject rule on `phase6-port/*` branches (operational enforcement of the "integration owns shim infrastructure" invariant): reject any commit that stages `shim/include/applet_indices.h`, `shim/include/HemispheresFactory.h`, `shim/include/PhzIcons.h`, or any file under `shim/include/`, `shim/src/`, `shim/src/compiler_rt/`. These files are owned by Layer 2 integration on the feature branch; applet implementers must never touch them. The hook makes this physical, not just instructional.

## Required skills + rules

- `superpowers:using-git-worktrees`. Step zero.
- `superpowers:brainstorming`. Drives the brainstorm.
- `superpowers:writing-plans`. Drives spec + plan.
- `superpowers:subagent-driven-development`. Drives Layer 1 parallel dispatch.

Load + honor `~/.claude/rules/parallel-execution.md`. Inline the worktree-dispatch checklist into the plan.

## Frozen recipes (per class)

The three test recipes below are frozen in this kickoff prompt per the Phase 5 carry-forward discipline ("Helper-design as discrete preflight deliverable, not brainstorm output"). The brainstorm assigns each applet to a class; the spec uses the corresponding recipe. The brainstorm does not redesign recipes.

### Recipe: Vendor-dep applets (Class A)

Applets that exercise a Phase 5 dep surface (VectorOscillator, Lorenz, tideslite, CVInputMap). Test pattern:

- Round-trip test: pack/unpack via `pack_<applet>` helper proves OnDataRequest bit layout is correct.
- Setup-state test: `Start()` defaults match vendor; `Controller()` observables after a few step() calls match expected for known inputs.
- Dep-surface test: at least one assertion proving the applet actually uses its dep correctly. For vec-osc applets, sample a VectorOscillator output and verify it falls in the expected range for a known waveform + phase increment. For Lorenz, verify the integrator produces non-constant output. For tideslite, verify slope/shape parameters affect the sample.
- Tolerance class: float-using deps use the documented tolerance from Phase 5's per-dep classification (typically 1 LSB of integer-converted output). Pin expected bytes for integer-only deps.

Each applet's test uses `step_n_frames(loaded, alg, bus, N)` with N ≤ 32 unless the applet has clock-driven state evolution, in which case follow the Class C recipe instead.

Per-applet entries cite vendor `Start()` and `Controller()` with `file:line` citations per the spec discipline.

### Recipe: Quantizer applets (Class B)

Applets that exercise the `HS::Quantize` pool on `HemisphereApplet` (the Phase 5 dep-quant base-class touch). Test pattern:

- Round-trip test: pack/unpack helper for the applet's state, including scale index and root note where stored.
- Quantizer-init test: verify the applet's `Start()` initializes the quantizer pool slot for its hemisphere. Vendor sets scale via `HS::SetScale(hem, scale_index)`; the test confirms the slot has the expected scale after Start().
- Quantization test: drive a known unquantized pitch through the applet and assert the output is the closest quantized note in the applet's scale. Chromatic scale (scale 0) is the simplest probe: input pitch should round to nearest semitone.
- Scale-change test: if the applet exposes scale as a parameter, change scale and verify subsequent quantization tracks the new scale.

Phase 5 shipped scale tables for ~30 named scales; tests must reference scales by name (e.g., "Chromatic", "Major") not by numeric index, since indices may shift if the table is reordered.

### Recipe: Helper-using cat-C applets (Class C)

Applets whose state evolution depends on `OC::CORE::ticks` advancing across many inner ticks (clock-driven counters, time-based gating). Test pattern uses `step_n_inner_ticks(loaded, alg, bus, N)` from the Phase 5 helper.

- Round-trip test: pack/unpack helper (standard pattern).
- Tick-budget test: pick N appropriate to the applet's clock-driven semantics. For ResetClock, N = one full reset cycle at the slowest mode (probably 1000-10000 inner ticks; choose based on vendor `RC_TICKS_PER_MS` math). For Shuffle/Xfader/Scope, choose N to traverse one full state cycle.
- Pair the tick-budget test with a short-N control test (N = 10-32) to confirm short-N behavior matches what `step_n_frames` would produce. Catches the failure mode where helper at large N drifts from helper at small N.
- Assertions on `OC::CORE::ticks` delta: confirm the helper advanced ticks by exactly N. The Phase 5 helper invariant test proves this in isolation; per-applet tests confirm it under load.
- 10x clocked-multiplier rule still applies: bus-level fire-count assertions against the multiplied Clock(ch) are unreliable. Use round-trip + state-injection + per-inner-tick channels sampled directly.

ResetClock is the natural first cat-C probe because it exercises both clock-driven state and ticks advancement. The Phase 4 abort report at `docs/superpowers/abort-reports/2026-05-18-resetclock-spec-mismatch.md` is authoritative for ResetClock semantics: (a) Clock(1) is the external Reset input not Clock(0); (b) Reset fires on `pending_clocks == 1` at output time not `position == offset`; (c) `Start()` does not reset `offset_mod`; (d) first-tick `UpdateOffset` jumps `pending_clocks` by the offset value; (e) closest analogue is Burst (pending-pulse queue with `RC_TICKS_PER_MS` timing), not ClkToGate. The ResetClock spec entry must cite all five.

### Recipe: Clock-mgr immediate applets (Class D)

Applets that exercise the full `ClockManager` surface shipped in Phase 5's dep-clock-mgr (BPM, multiply, swing, Tock, Cycle, EndOfBeat). Currently just Metronome.

- Round-trip test (standard pattern).
- ClockManager-state test: confirm `clock_m.SetTempoBPM(120)` followed by a known tick advance produces the expected Tock(ch) sequence. The step() prologue hook (Layer 0b in Phase 5) advances clock_m alongside `OC::CORE::ticks`; the test confirms this happens.
- Pair with a tick-budget test using `step_n_inner_ticks` if the applet's state evolves over many ticks.

If Phase 6 trims Class D to just Metronome, this recipe collapses to "round-trip + clock-state probe."

## Section markers convention

Phase 4's section markers proved out: `// === BEGIN <slug> ===` and `// === END <slug> ===`. Layer 0 of Phase 6 places markers for each in-scope applet in `harness/tests/test_hemispheres.cpp` and (where the applet has a pack helper) in `harness/tests/applet_test_helpers.h` and `.cpp`. Implementers commit only within their slug's markers; pre-commit hook verifies.

Implementer-side check before commit: `grep -c "// === BEGIN <slug> ===" harness/tests/test_hemispheres.cpp` returns exactly 1. Same for END marker. Build catches misplaced content via `make test-applets` failing.

## Operational applet-port boundary

The brainstorm defines the applet-port boundary with concrete predicates. Draft predicates:

1. Vendor unmodified rule: applet ports cite vendor source verbatim; the shim adapts if vendor headers don't compile. Phase 5 already shipped the dep surface, so vendor-dep applets should compile out of the box.
2. Test class assignment: each applet is exactly one class (A, B, C, or D). If an applet's behavior spans classes (e.g., a quantizer applet that's also clock-driven), the brainstorm picks the dominant class and documents the trade-off.
3. Recipe inheritance: each per-applet spec entry references the recipe class from this kickoff. The spec does not redefine recipes per applet.
4. Class-C demotion threshold: if more than 25% of any class's candidates demote during brainstorm or audit, halt and assess the class boundary. For Class C with 4 candidates, 2 demotions trigger halt. For Class B with 14, 4 demotions trigger halt. For Class A with 9, 3 demotions trigger halt. Class D with 1 has a 1-demotion halt.
5. No applet ports modify Phase 5 dep surfaces. The pre-commit hook enforces this.

## Phase 6 in-scope candidates (audit reconciles to final list)

The 28 named candidates above. The audit re-reads each vendor file to confirm fit at the Phase 5 shim surface. Each candidate audit produces:

- Class assignment (A, B, C, or D).
- Vendor source path with line count.
- Required Phase 5 dep surface (which deps the applet exercises).
- OnDataRequest bit layout summary.
- Recipe class match: confirms the frozen recipe covers the applet without extension.
- Status: in scope for Phase 6 / deferred for re-audit / deferred for Phase 7+ infrastructure work / spec defect to address.

If any candidate audit reveals nontrivial vendor edits would be required to compile against the Phase 5 shim, surface as a finding before brainstorm. Either the Phase 5 dep surface has a gap (Phase 6 abort, surface to user for Phase 5b decision) or the applet needs descope.

If the audit confirms 25 candidates fit and 3 demote (Drum1, Drum2, ASR-Drums to re-audit), proceed.

## Out of scope (defer to Phase 7+)

- Drum1, Drum2, ASR-Drums (vec-osc family; re-audit against Phase 5 shim surface in Phase 7 audit).
- ClockSetup variants and open-ended clock-aware applets (long-term; Phase 7+ enumerates).
- Any new dep ports. Phase 5 froze the dep surface; Phase 6 does not extend it.
- Vendor SDK changes (`vendor/distingNT_API` stays at the Phase 5-confirmed pin).
- The vendor commit `7800d929` pin stays.

## Vendor pin

Record the current pinned SHA of `vendor/O_C-Phazerville` at the top of the brainstorm. Phase 5 confirmed at `7800d929`. Re-audit applet inventory if changed.

## Existing ports + shim are the primary reference

Read these files in full before drafting:

- `applets/Hemispheres.cpp` (registration macro + test seam).
- `shim/include/applet_indices.h` (31 real applets + Empty at Phase 5 baseline).
- `shim/include/HemispheresFactory.h` (registration table).
- `shim/include/HemisphereApplet.h` (base class with Phase 5 dep-quant edits).
- `shim/include/HSUtils.h`, `HSClockManager.h`, `Arduino.h`, `hemispheres_shim.h`, `OC_core.h`, `util/util_math.h`, `util/util_macros.h`.
- All Phase 5 dep shims: `shim/include/vector_osc/`, `shim/include/lorenz/`, `shim/include/tideslite/`, `shim/include/quant/`, `shim/include/cv_map/` (paths as Phase 5 shipped them; confirm via `find shim/include -type d`).
- `shim/src/compiler_rt/` (vendored PIC builtins; Phase 6 does not modify).
- `harness/tests/applet_test_helpers.{h,cpp}` (Pack helpers + `step_n_frames` + `step_n_inner_ticks`).
- `harness/tests/test_hemispheres.cpp` (157 cases at Phase 5 baseline).
- All Phase 5 dep test files: `harness/tests/test_dep_<slug>.cpp` for each dep.

## Preflight report

After audit, post a preflight report with:

- Pinned `vendor/O_C-Phazerville` SHA (confirm `7800d929`).
- `~/.claude/rules/parallel-execution.md` status.
- Phase 5 baseline reverified at `8f42965`:
  - Applet count: 31 (Empty + 30 named).
  - `make test-applets`: 157 cases / 1816 assertions green.
  - `make test-deps`: 60 cases / 8564 assertions green.
  - `make arm`: 0 errors, exactly 3 accepted warnings (cxx_runtime_stubs.cpp: 1 -Wpragmas + 2 "operator new must not return NULL").
  - Hardware smoke check status (Phase 5 trio).
- Candidate audit:
  - Class A: 9 candidates audited, status each (in-scope / demote).
  - Class B: 14 candidates audited, status each.
  - Class C: 4 candidates audited, status each. ResetClock specifically: confirm 5 facts from abort report still trace to vendor source.
  - Class D: 1 candidate (Metronome) audited, status.
  - Deferrals: 3 vec-osc re-audit (Drum1, Drum2, ASR-Drums) — confirm these need re-audit, do not include in Phase 6 inventory.
  - Final in-scope count after audit (target: 25; report actual).
- Section marker count: number of marker pairs Layer 0 will place (one per in-scope applet, plus possibly per pack helper).
- Compiler-rt helper coverage check: build `make build/arm/Hemispheres.o` after Layer 0 commits and verify no new unresolved EABI helpers surface beyond Phase 5's set. Phase 6 applets may pull new helpers (e.g., quantizer code may use `__aeabi_dadd` for pitch math, tideslite may use `__aeabi_d2lz`). If new helpers surface, the parent agent extends `COMPILER_RT_SOURCES` in the Makefile BEFORE Layer 1 dispatch, vendoring additional compiler-rt files as needed. This is a Layer 0 work item, not Layer 1.
- Confirm `make test-applets` + `make arm` pass on `dr/phase6-applets-plan` at branch creation.

If any audit finding requires modifying Phase 5 dep surfaces, halt and surface. Phase 5 deps are frozen.

## Brainstorm requirements

- Vendor pin at top.
- Per-applet: class assignment, vendor file path, OnDataRequest bit layout summary, required Phase 5 dep, recipe class match, status.
- Final in-scope inventory committed: applet name, class, recipe class. The "28 named / 25 confirmed" discrepancy resolved explicitly.
- Class boundaries: confirm each applet's class is unambiguous. Borderline cases (quantizer applet that's also clock-driven) get explicit class-pick reasoning.
- Phase 7+ carry-forward: which applets defer, with reason (re-audit / long-term clock-aware / infrastructure work).
- ResetClock 5-fact verification: confirm each of the five facts from the abort report still traces to vendor source.

## Spec requirements

- Inherit the recipe sections from this kickoff verbatim (do not re-derive).
- Per-applet entries: applet name, class, recipe class, vendor file path with line range, status, OnDataRequest bit layout with `file:line` citations, `Start()` defaults with `file:line` citations, `Controller()` observables with `file:line` citations, Test concerns describing tests with recipe-class reference and any 10x-multiplier handling, Shim prereqs (which Phase 5 deps the applet uses), closest already-ported analogue.
- Per-applet entries are terse (one paragraph, max 12 lines including bullets). No paste-verbatim test bodies.
- Spec footer: Recipe spot-check (3 claims across the 4 recipe classes) + Per-entry verification (3 randomly-selected entries traced end-to-end against `Start()` and `Controller()`; if >1 of 3 wrong, audit every entry).

## Plan requirements

- Start with Dependency declaration citing `~/.claude/rules/parallel-execution.md`.
- Worktree-dispatch checklist invocation as first step.
- Pre-commit hook content with `phase6-port/*` patterns + `dr/phase6-applets-plan` accept-list + hard-reject for shim infrastructure files.
- DAG structure:
  - Layer 0 (parent, sequential): section markers in `test_hemispheres.cpp` and `applet_test_helpers.{h,cpp}`, pre-commit hook update, ResetClock spec inheritance from abort report (place in spec entry, not in a separate doc).
  - Layer 0.5 (parent, sequential, after Layer 0): per-applet pack helper forward declarations. Skeletons only; bodies in Layer 1. Allows implementers to compile their tests against forward-declared helpers.
  - Layer 1 (parallel): all in-scope applets dispatched in a single message. ~25 implementers.
  - Layer 2: integration on `dr/phase6-applets-plan`. Owns `applet_indices.h`, `HemispheresFactory.h`, `PhzIcons.h` edits + any vendor macro shims (unlikely; Phase 5 deps shipped them).
  - Layer 3: verification.
- Each Layer 1 implementer task block names: worktree path, branch name, base branch (`dr/phase6-applets-plan`), allowed surface (test_hemispheres.cpp within slug section markers, applet_test_helpers.{h,cpp} within slug section if pack helper needed), forbidden surface (everything else, especially shim infrastructure and other applets' sections), vendor source path, required Phase 5 dep, recipe class to apply, expected commit message, abort triggers.
- Plan length under 700 lines (25 applets is bigger than prior phases; budget relaxed from Phase 5's 600).
- Spec coverage check at the end: `| Applet | Class | Recipe | Spec entry | Task |` Markdown table.

## Hard constraints

- Plan declares parallel vs sequenced tasks explicitly.
- Each implementer's allowed surface is bounded and hook-enforced.
- No applet implementer modifies Phase 5 dep surfaces, shim files, or compiler-rt. Layer 2 owns integration files only.
- Recipe classes are frozen; spec uses them without modification.
- 10x clocked-multiplier rule applies. Each per-applet test entry states recipe class explicitly and acknowledges multiplier handling if class C/D.
- Section markers checked at implementer-side `make test-applets` before commit; build catches misplaced content.
- `make test-applets` baseline (157 cases / 1816 assertions) MUST pass at end of Layer 2 against the existing 31 applets. Phase 6 adds new applet tests; final count is 157 + (≈25 × test_cases_per_applet).
- `make test-deps` baseline (60 cases / 8564 assertions) unchanged. Phase 6 does not touch dep tests.
- `make arm` warning count remains exactly 3 (cxx_runtime_stubs). New warnings = regression.
- ARM build error count stays 0.

## Lessons inherited from Phase 5

Apply these without re-deriving. CLAUDE.md at HEAD encodes them:

- Compiler-rt builtins vendored at `shim/src/compiler_rt/` for PIC-correct ARM linking. Do not re-introduce libgcc extraction. If Phase 6 surfaces new EABI helper needs, vendor additional compiler-rt files; do not modify the existing vendoring approach.
- When ARM build surfaces "relocation of non-loaded section" or related errors, diagnose with `objdump -r` and `objdump -h` first. Stripping sections without understanding which relocation is the source is a debugging anti-pattern.
- Vendor compat headers usually compile against shim as-is. Check before stubbing.
- `Proportion` is a free function in `util/util_math.h`. Don't shadow it.
- Submodule init per new worktree: `git submodule update --init --recursive --depth=1`.
- Single-shot `set_gate` tests need `clear_bus` between steps.
- IOFrame `arr[4] = { -1 }` only initializes first element.
- Section markers + parent-agent integration workflow proven through Phase 4 and Phase 5. Default to it for Phase 6.
- Phase 5 helper invariants both green (Cumulus equivalence at N=10; tick-advancement at N=1000 against Empty + held gate). Phase 6 cat-C applets inherit a proven helper.

## Output paths

- Brainstorm: `docs/superpowers/brainstorms/YYYY-MM-DD-phase6-applets-brainstorm.md`.
- Spec: `docs/superpowers/specs/YYYY-MM-DD-phase6-applets-design.md`.
- Plan: `docs/superpowers/plans/YYYY-MM-DD-phase6-applets-plan.md`.

Use the date the document is first written.

## Abort budget

Surface immediately to the user + halt if:

**During audit (before preflight):**

- `~/.claude/rules/parallel-execution.md` missing worktree-dispatch checklist.
- Phase 5 baseline regression: any of the inheritance metrics (157/1816 applets, 60/8564 deps, 3 arm warnings, 0 errors) drift at `dr/phase6-applets-plan` branch creation.
- Vendor SHA changed and applets moved.
- Any candidate audit reveals a Phase 5 dep surface gap that would require dep modification (Phase 5 dep surfaces are frozen; this is a Phase 5b decision, not Phase 6).
- Compiler-rt helper coverage check reveals new EABI helpers, and the vendoring extension is non-trivial (>2 new compiler-rt source files needed). Surface for explicit Layer 0 decision.

**During planning:**

- Class boundary cases cannot be cleanly assigned. More than 2 applets are borderline between classes.
- Per-class demotion threshold tripped during brainstorm: >3 of 9 Class A applets demote, OR >4 of 14 Class B applets demote, OR >2 of 4 Class C applets demote, OR Class D's Metronome demotes (n=1). Each triggers class-specific halt: "boundary for class X is wrong, or candidate pool was overestimated."
- Total Phase 6 in-scope count drops below 18 applets after audit + brainstorm (well below the 25 confirmed target).
- Total Phase 6 in-scope count exceeds 30 applets (single fan-out scale risk).
- Plan exceeds 700 lines.

**During Layer 0/0.5:**

- Section marker placement breaks any existing test in `make test-applets`.
- Pack helper forward declarations break `make test-applets`.
- `make arm` adds new warnings beyond the 3 accepted.
- Pre-commit hook fails to reject a test commit that stages `shim/` paths or other applets' section regions.
- New EABI helper surfaces in Hemispheres.o that the existing compiler-rt set doesn't cover (must be caught at preflight per compiler-rt coverage check; this is a backup catch).

**During Layer 1:**

- More than 3 implementer subagents abort substantively (proportional to scale; Phase 4 had 2, Phase 6 at 3-4x scope has 3 as the threshold).
- Any implementer commits files outside the allowed surface despite the hook.
- Any implementer modifies a Phase 5 dep surface (shim/include or shim/src under dep area).
- Class-specific abort: more than 25% of any class's implementers abort substantively. Class A: 3+, Class B: 4+, Class C: 2+, Class D: 1.
- Fan-out wallclock exceeds 8 hours (Phase 4 was ~50 min for 7 applets; 25 applets at proportional scale is 3-4 hours; 8h budgets for friction).

**During Layer 2:**

- `make test-applets` regresses any existing test (any of the 157 baseline cases or 1816 assertions).
- `make test-deps` regresses (60/8564 baseline). Phase 6 should not touch dep tests.
- `make arm` produces more than 3 warnings or any errors.
- Integration commit triggers Layer 1 implementer's section-marker mismatch (misplaced content).

**During Layer 3:**

- Hardware smoke check on Phase 5 trio regresses vs Phase 5 baseline (signal that Phase 6 broke shared infrastructure).
- Hardware smoke check on Phase 6 newly-landed pair reveals behavior mismatch with host tests.

## Reporting

End-of-run message:

1. Documents produced (paths).
2. Phase 6 scope: applet count by class (A/B/C/D), total landed.
3. Layer 0 + Layer 0.5 commits with SHAs.
4. Layer 1 result: success count, abort count with reasons, wallclock.
5. Layer 2 integration: pass/fail.
6. Layer 3 final verification:
   - `make test-applets`: case + assertion count, delta vs 157/1816 baseline.
   - `make test-deps`: unchanged at 60/8564 (confirm).
   - `make arm`: 0 errors, 3 warnings (confirm).
   - Hardware smoke: Phase 5 trio status + Phase 6 pair status.
7. PR URL.
8. Phase 7+ carry-forward inventory: deferred applets (Drum1/Drum2/ASR-Drums for re-audit; clock-aware long-term set) + any Phase 6 abort applets.

If an abort fired, post the abort report instead.

## Success criteria

- Three documents exist at declared paths.
- Layer 0 commits land cleanly with baseline green between each.
- Layer 1 ships ≥18 applets (allows for 2-7 demotions from the 25 confirmed target without phase abort).
- All shipped applets have passing host tests in `make test-applets`.
- `make test-applets` final count matches expected: 157 + (sum of per-applet test cases). Document expected count in the plan.
- `make test-deps` unchanged at 60 cases / 8564 assertions.
- `make arm` 0 errors, exactly 3 accepted warnings.
- Hardware smoke check on Phase 5 trio still green (Binary or Switch, ShiftGate or Voltage, ClockSkip or RndWalk) + at least one Phase 6 newly-landed pair (one cat-C and one Class A or B, to exercise the helper-using and quant pool paths on hardware).
- PR open on `dr/phase6-applets-plan` with Load-bearing decisions summary including:
  - Final in-scope count and resolution of the 28-vs-25 candidate math.
  - Per-class demotion outcomes.
  - ResetClock 5-fact verification confirmed in spec.
  - Any mid-Layer-1 abort handling.
  - Compiler-rt extension decisions if any.

## First actions

1. Confirm `.worktrees/phase6-applets-plan` exists on branch `dr/phase6-applets-plan` from `main` at `8f42965`. If not, parent provisions via `git worktree add .worktrees/phase6-applets-plan -b dr/phase6-applets-plan 8f42965` and runs `git submodule update --init --recursive --depth=1`.
2. Run `make test-applets` + `make test-deps` + `make arm` to confirm Phase 5 baseline green. Record exact case + assertion + warning counts.
3. Read primary references in full: shim base class headers (especially HemisphereApplet.h with Phase 5 dep-quant edits), Phase 5 dep shims, time-injection helper API, ResetClock abort report.
4. Read Phase 5 brainstorm/spec/plan + carry-forward inventory.
5. Verify `~/.claude/rules/parallel-execution.md` worktree-dispatch checklist is present.
6. Audit each candidate applet's vendor file against Phase 5 dep surface. Assign class. Confirm 5-fact match for ResetClock.
7. Run compiler-rt helper coverage check via stub Hemispheres.o build with Phase 6 candidates included.
8. Post preflight report.
9. Write brainstorm, spec, plan.
10. Execute Layer 0 (section markers + hook + ResetClock spec inheritance).
11. Execute Layer 0.5 (pack helper forward declarations).
12. Execute Layer 1 (all in-scope applets parallel dispatch in one message).
13. Execute Layer 2 (integration: applet_indices.h, HemispheresFactory.h, PhzIcons.h alpha-ordered inserts + pack helper bodies if not in Layer 1).
14. Execute Layer 3 (verification: host tests, ARM build, hardware smoke check).
15. Open PR.
16. Post final report.

If any abort condition fires, halt + surface immediately.
