// Output-parity class: mixed. Tempo math is float; tick advancement is
// integer. Per-method classification documented in the dep-clock-mgr
// invariant test that replaces this placeholder.
// dep-clock-mgr placeholder (Phase 5 Layer 0a). The dep-clock-mgr
// implementer replaces this with the real ClockManager invariant test per
// the spec entry.
#include "catch.hpp"

TEST_CASE("dep-clock-mgr placeholder", "[dep-clock-mgr]") {
    CHECK(true);
}
