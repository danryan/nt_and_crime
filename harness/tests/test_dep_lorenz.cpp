// Output-parity class: integer-only, byte-identical across host and ARM.
// dep-lorenz placeholder (Phase 5 Layer 0a). The dep-lorenz implementer
// replaces this with the real LorenzGeneratorManager invariant test per
// the spec entry.
#include "catch.hpp"

TEST_CASE("dep-lorenz placeholder", "[dep-lorenz]") {
    CHECK(true);
}
