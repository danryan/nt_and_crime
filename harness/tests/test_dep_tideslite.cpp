// Output-parity class: mixed. Integer-only for ComputePhaseIncrement and
// PhaseExtractor; float-tolerance (1 LSB) for ProcessSample.
// dep-tideslite placeholder (Phase 5 Layer 0a). The dep-tideslite implementer
// replaces this with the real tideslite and PhaseExtractor invariant tests
// per the spec entry.
#include "catch.hpp"

TEST_CASE("dep-tideslite placeholder", "[dep-tideslite]") {
    CHECK(true);
}
