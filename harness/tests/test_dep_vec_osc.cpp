// Output-parity class: float-using, 1-LSB tolerance on final int16 output.
// dep-vec-osc placeholder (Phase 5 Layer 0a). The dep-vec-osc implementer
// replaces this with the real VectorOscillator and RelabiManager invariant
// tests per the spec entry.
#include "catch.hpp"

TEST_CASE("dep-vec-osc placeholder", "[dep-vec-osc]") {
    CHECK(true);
}
